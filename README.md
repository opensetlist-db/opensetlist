# OpenSetlist

Crowdsourced setlist database for East Asian live music — K-POP, J-POP, C-POP, anime, game, and beyond.

동아시아 라이브 음악(K-POP·J-POP·C-POP·애니메이션·게임 등)을 위한 크라우드소싱 세트리스트 데이터베이스.

[opensetlist.com](https://opensetlist.com)

## What it does

- **Per-event setlists** with per-song unit/member credits — the thing setlist.fm doesn't model.
- **Artist hierarchy** for groups, sub-units, and solo acts; stage identities (characters / personas) kept separate from voice actors so VA changes don't rewrite history.
- **Real-time setlist polling** during ongoing events with a pulsing LIVE badge.
- **One-line impressions** ("한줄감상") per event with on-demand translation between viewer locales.
- **Dynamic OG cards** with data-derived glassmorphism palettes and self-hosted CJK fonts for Twitter / KakaoTalk / Discord previews.
- **i18n from day one**: Korean (launch), Japanese, English, Simplified Chinese (Phase 3).

## Tech Stack

- **Framework:** Next.js 14 (App Router) + TypeScript (strict)
- **i18n:** next-intl with `/[locale]/` routing
- **ORM & DB:** Prisma 7 + PostgreSQL (Supabase Seoul)
- **Hosting:** Vercel (frontend) + Cloudflare (CDN)
- **Translation:** OpenAI Responses API (prod) / Google Gemini (preview)
- **Observability:** Sentry, GA4

## Contact

help@opensetlist.com

## Release Notes

### v0.11.4 (2026-05-12)
- **Phase 1B mobile polish + share-card lifecycle expansion + clipboard-copy as a second share CTA — six PRs spotted across an iPhone Safari spot-check of the v0.11.1 deploy, a MacBook share-card walkthrough, and a Korean-community-site sharing-workflow observation.** No schema migration, no new env vars, no new runtime deps. Five bug fixes + two feature expansions (the share button now works pre-show + during the show, not only post-show; the share-card modal now offers a clipboard-copy button alongside the existing download/share button so users can paste straight into community-site composers and messengers). Earlier this calendar day, an aborted v0.11.3 release branch was opened with the first five PRs (#335–339) only; closed in favor of bundling everything into v0.11.4 so the release pattern returns to dev → main directly per the older convention (PRs #266–#315) rather than via an intermediate release/vX.Y.Z branch.
- **#335 — Mobile setlist position cell tightened from 64px → 52px.** iPhone Safari (390px viewport) was wrapping the 4th emoji reaction onto a second line because the mobile position column was 64px (12px wider than the actual content — the ✓+✕ dual-button block is 22+8+22 = 52px exactly). The 12px of slack squeezed the `1fr` reactions track from 294px down to 282px, just under the ~285px the four buttons need on iOS Apple Color Emoji metrics. Shrinking the mobile grid template from `[64px_1fr]` → `[52px_1fr]` reclaims those 12px. Confirmed-row position numbers (1–2 digits, right-aligned) still fit cleanly inside 52px. Desktop column unchanged (already 52px in `setlistLayout.ts`).
- **#336 — Confirm-vote instructional description moved from the top of the setlist body to the bottom.** v0.11.1 placed the `새로 등록된 곡이 정확하면 ✓를, 아니면 ✕를 눌러 주세요.` strip above `<SetlistColumnHeader>`. Operator pointed out that new rumoured rows are appended at the bottom of the list as they're added, so a top-of-list placement put the help copy far away from the rows whose ✓/✕ buttons it explains once the list had any length. Strip now sits after the main + encore `<ol>`s with `borderTop` (was `borderBottom`) so it reads as a "footer answer" to the column header at the top of the same block. Same `hasRumoured` visibility gate; same copy.
- **#337 — Share-card "이미지 저장" force-downloads on desktop even when `canShare({files})` succeeds.** macOS Safari + macOS Chrome (16.x+) both pass `navigator.canShare({ files: [pngFile] })`, so v0.10.2's `#306` capability-gated share path routed MacBook users into the OS share sheet — which on macOS offers only Mail / Messages / AirDrop / Notes / etc., with no "save to Downloads" entry. Operator-spotted: a viewer tapped 이미지 저장 expecting a saved PNG and got stuck in a sharing-only sheet. Added a `(pointer: coarse)` media-query gate to the share branch in `src/lib/shareCard.ts` — the share path now requires **touch-primary device AND file-share capability**. Desktops (mouse / trackpad) fall through to the existing download path regardless of capability. iPad with Magic Keyboard trackpad attached reports `pointer: fine` and correctly gets the desktop UX. New regression test stubs `(pointer: coarse) → false` + `canShare → true` and asserts `navigator.share` is never invoked.
- **#338 — SongSearch mobile dropdown position fix.** Operator-spotted on iPhone: typing in the wishlist or predict search input made the dropdown end up off-screen, recoverable only by scrolling. Root cause: the portalled dropdown used `position: fixed` (PR #290) so it would track the input through scroll + visualViewport changes, but iOS Safari handles `position: fixed` unreliably when the soft keyboard is open — fixed elements either fail to track the visual viewport or get pulled along by scroll, depending on state. Switched to `position: absolute` with **document-relative coords** (`rect.bottom + window.scrollY + 4`): the dropdown now lives at a fixed point in the document and scrolls naturally with the input (also in document flow), so iOS's fixed-positioning quirk no longer applies — there's no fixed-viewport-anchor to misinterpret. The portal-to-body strategy stays in effect (the dropdown still escapes ancestor `overflow: hidden`). The `scroll` / `resize` / `visualViewport` listeners stay because **layout reflow** (keyboard appearing, content above expanding, orientation change) can still shift the input in document space and `update()` needs to fire then; scroll position itself no longer needs handling. CR caught a stale state-variable comment that still described "fixed-position coords" and "page scrolls behind the dropdown" phrasing — refreshed alongside.
- **#339 — Share button enabled at every lifecycle stage (pre / live / final).** v0.11.1-and-earlier only enabled share post-completion. v0.10.2's `#307` made the button visible-but-disabled during the show with a "공연 종료 후 활성화됩니다" hint, and pre-show returned `null` entirely. That missed the most viral entry point: fans share their predictions BEFORE the show, friends see the image, friends show up. v0.11.4 makes the share button visible + enabled in every state where the viewer has at least one prediction stored. Three card modes, derived via a new exported `deriveShareCardMode(status, hasActuals)` pure function: **`prediction`** (upcoming OR ongoing-with-no-actuals) — button reads `예상 공유 🎯`, card drops the score banner and renders predictions in the viewer's chosen rank order with a "예상 세트리스트" section label; **`live`** (ongoing + has actuals) — button reads `결과 공유 🎯`, card mirrors the post-show layout but adds a red `LIVE` pill in the top-right corner so a friend re-seeing the image later understands the numbers are mid-flight, score is partial; **`final`** (completed) — v0.10.x layout unchanged. Native-share text varies by mode (the prediction caption reads "내 예상 세트리스트 — N곡", the live caption reads "공연 진행 중 — 현재까지 N곡 중 M곡 적중 (X%)"). GA4 `share_card_open` gains a `mode` param so the pre-show share funnel is queryable separately from the post-show funnel — this is the new viral-attribution signal. `Predict.shareDisabled` is kept in messages but no longer referenced; left in place for any future "disabled" state without locale-key churn.
- **#342 — Image-copy-to-clipboard as a second CTA alongside download/share.** The share-card modal now renders two action buttons side-by-side directly below the preview, both 44px min-height for touch ergonomics (Apple HIG / Material minimum). **Button A (`다운로드 / 공유`)** is the existing `shareCard()` path — downloads PNG on desktop, opens OS share sheet on touch-primary devices that pass `canShare({files})`. Icon swaps dynamically (download-arrow on desktop, iOS-style share box on touch devices). **Button B (`이미지 복사`)** is a new `copyCardToClipboard()` path that writes the rendered PNG to `navigator.clipboard` as `image/png`. Users can then paste straight (Ctrl+V / ⌘V) into community-site composers (DCInside, Ruliweb), messengers (KakaoTalk, Slack, Discord), or anywhere else handling image paste — bypassing the "save → open gallery → attach" loop that was the canonical Korean-community-site sharing workflow. Capability-gated: Button B is hidden entirely when `window.ClipboardItem` or `navigator.clipboard.write` is unavailable (older Firefox, some embedded WebViews). **Load-bearing iOS Safari detail**: `ClipboardItem` is constructed with the *unresolved* `Promise<Blob>` from the new `renderCardToBlob` helper, not an awaited Blob — per W3C spec, this preserves the user-gesture context across the async html2canvas render. Awaiting first and then passing the resolved Blob throws `NotAllowedError` on iOS. A regression test pins the ordering via `typeof slot.then === "function"`. GA4 `share_card_save` outcome param extended to `'downloaded' | 'shared' | 'copied'`; `share_card_save_failed` gains `clipboard_unsupported` + `clipboard_denied` reasons. Copy-success toast `복사됐어요! 붙여넣기(Ctrl+V / ⌘V)로 사용하세요` mentions both shortcuts so Mac users see the ⌘V hint (CR-caught — first iteration was Windows-only Ctrl+V). `ShareCard.saveImage` relabelled to `다운로드 / 공유` / `Download / Share` / `ダウンロード / 共有` so the button's actual behavior matches its label (it does both, not just save). Icons are 3 inline SVGs — no new dependency.
- **Tests: 823 → 838 (+15).** +8 from #339 (6 `deriveShareCardMode` transition-table cases pinning `upcoming/ongoing±actuals/completed/cancelled±actuals` + 2 net-new button-gate cases — upcoming-with-predictions renders enabled, completed-with-no-actuals still renders). +7 from #342 (`copyCardToClipboard` happy path, unresolved-`Promise<Blob>` contract pin for iOS gesture preservation, `ClipboardItem` missing, `clipboard.write` missing, `NotAllowedError` → denied, arbitrary error, render-failure-through-ClipboardItem-promise). Existing `share-card.ts` suite gained one regression test for the macOS pointer-fine path (#337). Other PRs (#335, #336, #338) are surgical CSS / comment / glyph changes that the existing suites already cover structurally.
- **Operations footnote — no DB or env changes.** No schema migration, no new env vars, no new runtime deps. The 5/30 Kanagawa Day-1 `LAUNCH_FLAGS.confirmDbEnabled` activation remains a separate one-line commit on top of v0.11.4. Post-deploy smoke: spot-check on iPhone Safari that all four reactions stay on one line (#335) and the SongSearch dropdown appears immediately below the input on both wishlist + predict (#338); spot-check on MacBook Safari that 이미지 저장 downloads a PNG instead of opening the share sheet (#337) and 이미지 복사 paste works in Notes / Messages (#342); confirm the share button renders pre-show with the "예상 공유 🎯" label and the card has no score banner (#339); confirm during-show with at least one actual song renders the LIVE pill (#339); confirm 이미지 복사 paste works on iPhone Safari into KakaoTalk / Messages (#342).

### v0.11.2 (2026-05-11)
- **Two follow-up fixes from the v0.11.0 Realtime activation prod smoke. No new functionality.** The post-deploy smoke surfaced one DB-schema divergence and one Sentry tunnel-routing bug — both pre-existing, both surfaced by activation work. v0.11.2 closes both.
- **#332 — `SetlistItemReaction.eventId` NOT NULL re-applied (silently dropped twice via merge-revert lineage).** The NOT NULL tightening from PR #320 was supposed to ship to main via PR #327 (v0.10.5) and again via PR #329 (v0.11.0). Both merges silently dropped the schema change because git's three-way merge resolved the v0.10.4 revert (commit `d0030cd`, which had to revert #320 to ship the column nullable for safe initial rollout) as authoritative — main's revert "won" against release-branch diffs that derived from a merge base AFTER #320 was already in shared history. The release-PR diffs visibly included the schema change in the `+/-` listing, but the merge resolved it away silently. Discovered when v0.11.0's `migrate-prod.yml` ran clean but `prisma db pull --print` showed prod still at `BigInt?`. Operational impact was zero (the API populates `eventId` on every reaction INSERT since R2 / PR #316), but the divergence between `schema.prisma` (says NOT NULL after my comment block) and the actual prod state (still nullable) made the codebase confusing. Fix: branched off main directly (not dev) so the PR's diff is a fresh schema modification git can't trace back to the `d0030cd` revert lineage. The diff is a clean `BigInt? → BigInt` (plus dropping `?` from the `event Event?` relation) that the merge applies normally. Comment block now records the merge-lineage history so the next person debugging a similar situation finds the explanation in the codebase.
- **#333 — Sentry tunnel route `/monitoring` excluded from i18n locale prefixing.** Pre-existing middleware bug surfaced by v0.11.0's prod smoke (check 2 — Console clean): the next-intl middleware in `src/proxy.ts` was matching `/monitoring` and adding a locale prefix (e.g., `/en/monitoring`), which the framework's Sentry tunnel rewrite (configured via `tunnelRoute: '/monitoring'` in `next.config.ts`) doesn't match. Every Sentry browser-side telemetry envelope was 404ing as a result — and silently. Specifically blocked the production observability built into the Realtime hooks (`Realtime fallback to polling` `captureMessage` from `useRealtimeEventChannel` and `useRealtimeImpressions`, plus `Sentry.addBreadcrumb({ category: "realtime", … })` transition tracking — none reached the dashboard). Same blast radius for all browser-side Sentry events (errors, sessions, etc.). One-line fix: add `monitoring` to the matcher exclusion list (mirrors the existing exclusions for `api` / `admin` / `admin-login` / `_next` / `_vercel`). Comment block expanded to document why each exclusion exists, so future readers don't reverse-engineer it.
- **Migration impact** (`migrate-prod.yml` on the v0.11.2 tag): `prisma db push` runs `ALTER COLUMN "eventId" SET NOT NULL` on the existing column — succeeds cleanly because v0.10.4's backfill ensured zero NULL rows on prod. No `post-deploy.sql` changes (idempotent guards skip everything inert). No new env vars, no new runtime deps. Sentry envelopes start reaching the dashboard the moment the deploy lands — no DB or config changes needed for the tunnel fix.
- **Tests: 823 → 823 (unchanged).** Both fixes are surgical: schema-only change + middleware matcher addition. The existing suites (`reactions-route`, hooks tests) already assert the `eventId` populate behavior; no new tests needed for the schema constraint tightening.
- **Operations footnote — no behavioral change beyond restored observability + DB constraint integrity.** The activation done in v0.11.0 stays as the meaningful behavior shift; v0.11.2 just brings prod's actual state into alignment with what `schema.prisma` describes and restores the Sentry pipeline that's been silently broken since middleware + tunnel were introduced separately.

### v0.11.1 (2026-05-11)
- **Phase 1B UX polish — first-time-visitor instructional copy on the three participatory surfaces, plus a Confirm glyph swap that fixes a long-standing semantic mismatch.** No new functionality, no schema migration, no new env vars or runtime deps. Single-PR release on top of v0.11.0's Realtime activation.
- **#324 — Instructional descriptions on Wishlist / Predicted Setlist / Confirm.** Each of the three Phase 1B surfaces gained a small muted hint strip explaining the feature's rules — until now the title + action button stood alone and a first-time visitor had to infer the contract from the affordance shape. Each description is gated to the editable/relevant state so it retires when the action it describes is no longer possible: Wishlist + Predicted Setlist show pre-show (`!isLocked` / `isPreShow`); Confirm shows only while at least one row is still rumoured (reuses the existing `hasRumoured` flag that drives the auto-promote ticker). Wishlist's standalone `최대 3곡` cap badge was removed in favor of the new description sentence that says the same thing — header chrome (title row + description row) reads as one block with `wishlistBorder` tying them visually. Strings added to ko / en / ja in lockstep; the `Wishlist.cap` key was deleted from all three locale files. `event-wish-section` tests updated from `cap` → `description` assertions.
- **#324 (same PR) — Confirm vote buttons: 👍/👎 → ✓/✕.** The thumb-up/thumb-down glyphs shipped with the v0.10.1 `<NumberSlot>` rewrite read as **like / dislike** across the consumer web (YouTube, Reddit, Facebook), the wrong mental model for a "this row is factually correct" vote. A fan could 👎 a song they didn't enjoy even when the song WAS performed, polluting the data the surface exists to produce. Swapped to monochromatic text symbols `✓` (U+2713) / `✕` (U+2715) — they inherit the button's `color` style, so the active-state palette (sky-blue confirm, rose-red disagree) carries the factual claim instead of being undermined by the glyph's emotional register. Font bumped from `text-[11px] font-medium` → `text-[14px] font-bold` since text symbols at 22×22 need more weight than the dense emoji shapes they replace. `aria-labels` (`정확함` / `잘못됨`) were already factually-worded — only the visible icon needed to change. Stale `👍`/`👎` comments updated in 5 source files + 3 test files; README release notes preserved as-is since they document the shipped state at v0.10.1.
- **Tests: 823 → 823 (unchanged).** Pure UI + copy changes; the affected suites (`event-wish-section`, `numberSlot`, `setlist-row`, `useLocalDisagree`) saw assertion updates (e.g. `toBe("👍")` → `toBe("✓")`) but no new or removed test cases. Total passing on dev at the v0.11.1 tag: 823 / 823.
- **Operations footnote — no DB or env changes.** No schema migration, no new env vars, no new runtime deps. The 5/30 Kanagawa Day-1 `LAUNCH_FLAGS.confirmDbEnabled` activation remains a separate one-line commit on top of v0.11.1.

### v0.11.0 (2026-05-11)
- **Phase 1C Realtime activation — `LAUNCH_FLAGS.realtimeEnabled` flag dropped, polling-path branches deleted in `LiveEventLayout` and `EventImpressions`.** This is the activation commit that mirrors the planned `confirmDbEnabled` activation pattern. The Realtime architecture (R1+R2+R3 + supporting fixes) shipped in v0.10.4 / v0.10.5 behind the flag; everything has been smoke-tested on dev with the flag flipped + verified end-to-end on prod operator-side. v0.11.0 makes Realtime unconditional: every live event page now uses `useRealtimeEventChannel` + `useRealtimeImpressions` as the primary data source. Push updates land in <1s instead of <5s, F14's connection-pool ceiling is structurally relieved (the highest-volume slice — reactions — drops to zero per-poll DB hits).
- **The polling hooks (`useSetlistPolling`, `useImpressionPolling`) stay alive as the in-hook R3 fallback path.** They take over automatically on `CHANNEL_ERROR` / `TIMED_OUT` (supabase-js's retry budget exhausted) within one render, invisibly to the audience. The only deletions are the call-site flag-on/off branches that were paying for the staged rollout — the actual fallback infrastructure is unchanged.
- **The `realtimeEnabled` flag entry is dropped from `src/lib/launchFlags.ts`** along with its docstring (which was load-bearing only during the rollout window). `LAUNCH_FLAGS` now contains just `showSignIn`, `showSearch`, and `confirmDbEnabled` — the latter still pending its own 5/30 Kanagawa Day-1 activation.
- **`LiveEventLayout` simplification.** Deleted: the `realtimeFlag: boolean` declaration, the dual-hook calls (one with `enabled: isActive && !realtimeFlag`, the other with `enabled: isActive && realtimeFlag`), the `realtimeFlag ? realtime : polled` ternary, and the `useSetlistPolling` value import. Kept: the `ReactionCountsMap` type re-export from `useSetlistPolling` (still used in props), the `startTimeIso` coercion (still needed for the realtime hook's boundary scheduler), and every downstream consumer comment about how `polledStatus` flows.
- **`EventImpressions` simplification.** Deleted: the `realtimeFlag` declaration, the `LAUNCH_FLAGS` import, the `(!realtimeFlag || realtime.pollFallback)` polling-enabled compound. Now `useRealtimeImpressions` is enabled unconditionally on `isOngoing` events; `useImpressionPolling` is enabled ONLY on `isOngoing && realtime.pollFallback` (R3 fallback). The `applyImpressionsUpdate` synchronous-ref-update helper that landed in v0.10.5's PR #326 is unchanged — same correctness story for the `totalCount` sync race.
- **Operator kill switch removed**, replaced by deploy revert.** Pre-activation, flipping `LAUNCH_FLAGS.realtimeEnabled = false` was a one-line emergency lever. Post-activation, the equivalent is a Vercel deploy revert (rolls back the v0.11.0 commit). The R3 in-hook fallback still handles per-channel CHANNEL_ERROR / TIMED_OUT gracefully — only a Realtime-service-wide outage would warrant a deploy revert, and even then the in-hook fallback would have already kicked in for affected viewers.
- **Tests: 823 → 823 (unchanged).** Activation is a deletion of branches that were already exercised by both code paths in the existing test suite. The realtime path's tests (added incrementally across R1/R2/R3) continue to pass; the polling path's tests still pass because `useImpressionPolling` and `useSetlistPolling` are still called (just with stricter `enabled` predicates).
- **Operations footnote — post-deploy smoke required.** Operator: after the v0.11.0 tag deploys, run the same two-browser smoke from the original step-6 checklist (admin SetlistItem insert → viewer A updates within ~1s; reaction tap → count increments via WebSocket frame, no `/api/reactions` or `/api/setlist` HTTP fires; impression POST + edit; impression hide via SQL UPDATE; SongWish push). Then verify the R3 fallback by blocking `wss://*.supabase.co/*` in DevTools Network → polling cadence resumes within ~10s. If anything regresses, the rollback path is a Vercel deploy revert of the v0.11.0 commit. The Realtime infra (env vars, publication tables, REPLICA IDENTITY) all stay configured on prod — no DB rollback needed.

### v0.10.5 (2026-05-11)
- **Schema-only follow-up to v0.10.4: tighten `SetlistItemReaction.eventId` to `NOT NULL`.** v0.10.4 shipped the column as nullable so the prod migration could `ADD COLUMN` cleanly on a populated table; v0.10.4's `post-deploy.sql` then backfilled every existing row from `setlistItem.eventId` and `POST /api/reactions` populates `eventId` on every new INSERT. With the column now reliably populated end-to-end on prod (operator verified `SELECT COUNT(*) FROM "SetlistItemReaction" WHERE "eventId" IS NULL` returned 0 post-v0.10.4 deploy), the column safely tightens to NOT NULL. Future refactors of the reactions POST that forget to populate `eventId` will fail loudly at INSERT instead of producing rows that silently bypass the Realtime `postgres_changes` filter — the column-level invariant is now DB-enforced.
- **#320 — `chore(schema): tighten SetlistItemReaction.eventId to NOT NULL` (re-applied).** Pure schema tightening: `eventId BigInt? → BigInt`, `event Event?` relation drops its `?`. The Event side has no `onDelete: Cascade` (chained via SetlistItem already, second cascade path would risk Postgres "multiple cascade paths" errors). Comment updated to reflect the post-tightening state — drops the "follow-up migration tightens this to NOT NULL" line that was load-bearing during the v0.10.4 rollout window. No application-code change needed; the reactions POST already populates `eventId` on every insert (free join from the existing parent SetlistItem lookup that validates existence).
- **Migration impact** (`migrate-prod.yml` on the v0.10.5 tag): `prisma db push` runs `ALTER COLUMN "eventId" SET NOT NULL` on the existing column — succeeds cleanly because v0.10.4's backfill ensured zero NULLs. No `post-deploy.sql` changes (R2's existing block stays inert on a re-run since the column is already populated). No new env vars, no new runtime deps.
- **Tests: 818 → 818 (unchanged).** Schema-only change; the existing reactions-route test suite already asserts `eventId` is passed in the create call.
- **Operations footnote — Realtime activation still pending.** v0.10.5 completes the Realtime infra rollout but `LAUNCH_FLAGS.realtimeEnabled` stays `false` — the activation PR (planned as v0.11.0) is the meaningful behavior shift. Two prep items remain before activation: (1) a focused dev-side fix PR addressing the deferred CR finding from #323 (closure-capture race in `EventImpressions` `onUpsert`/`onRemove` — bug only manifests when the realtime flag is on, so unblocked by the staged release approach); (2) one final two-browser smoke on a Vercel preview against prod after this tag deploys, confirming the `prisma db push` `SET NOT NULL` succeeded and reactions still work.

### v0.10.4 (2026-05-11)
- **Phase 1C Week 3 — Supabase Realtime architecture lands across six PRs (R1+R2+R3 + two boundary-cadence fixes + one home-card UI patch). Flag-gated default off; production behavior unchanged.** The release ships the entire push-subscription stack so every R1/R2/R3 piece can be activated via a single one-line follow-up PR (drop `LAUNCH_FLAGS.realtimeEnabled`) once dev verification + prod operator setup complete. `LAUNCH_FLAGS.realtimeEnabled = false` keeps `useSetlistPolling` (5s cadence) and `useImpressionPolling` (30s cadence) driving every event detail page exactly as today; flipping the flag swaps in `useRealtimeEventChannel` + `useRealtimeImpressions` (same return shapes, drop-in replacements) so push updates land in <1s instead of <5s and the F14 connection-pool ceiling is structurally relieved (reaction polling — the highest-volume slice — drops to zero per-poll DB hits). Activation PR is intentionally separate so reviewers see the behavior change alone, not bundled with the architecture.
- **#311 — R1: SetlistItem Realtime channel.** Per-event channel `event:{eventId}` carrying `postgres_changes` for SetlistItem, filtered by `eventId=eq.{eventId}`. Path B reconciliation (any push triggers a `/api/setlist` refetch) — chosen over per-row diff merge because the `postgres_changes` payload is the bare row, but `LiveSetlistItem` consumers (`<LiveSetlist>`, sidebar derivations) expect deeply-nested songs/performers/artists joined. Reconstructing that shape from individual table pushes is R2's problem; for R1 the push acts as a "kick" to refetch the joined snapshot, so latency drops from ≤5s (poll cadence) to ~100ms (push → refetch) without inventing a second source of truth. New `src/lib/supabaseClient.ts` lazy module-scope singleton (`createClient` deferred so a polling-only build that doesn't set the Supabase env vars still loads cleanly — anon-key-only, no auth session, no token refresh). New `useRealtimeEventChannel` hook mirrors `useSetlistPolling`'s API + return shape exactly. Both hooks always called inside `<LiveEventLayout>` (rules-of-hooks); `enabled` flag picks which one does work. `prisma/post-deploy.sql` adds `SetlistItem` to the `supabase_realtime` publication via an idempotent `DO`-block (pg_publication_tables guard). New deps: `@supabase/supabase-js`. New env vars: `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` (operator: set in Vercel Production before the activation PR ships).
- **#316 — R2: reactions / impressions / wishlist on push.** Extends the architecture to the four remaining tables. `SetlistItemReaction` uses Path A (per-row diff merge into `reactionCounts` — F14's structural relief lives here, the highest-volume slice goes from 5s polling to zero polling). `EventImpression` uses Path A via the new `useRealtimeImpressions` hook on a separate channel `event:{eventId}:impressions` (impressions feed has its own cursor + load-more state owned by `<EventImpressions>`, so the hook can't transparently swap the way `useRealtimeEventChannel` can — it exposes `pollFallback` in the return shape and `<EventImpressions>` enables `useImpressionPolling` alongside on the same `mergeImpressions` state). `SongWish` uses Path B refetch (TOP-3 needs locale-specific song-translation joins, awkward to mirror client-side; volume is low, ~10s of wishes per show). `SetlistItem` stays Path B from R1 (joined-shape mismatch unchanged). **Schema gap fix**: `SetlistItemReaction` lacked a direct `eventId` column, which Supabase Realtime's column-only `postgres_changes` filter requires — denormalized as nullable `BigInt?` with `post-deploy.sql` backfill from parent `SetlistItem.eventId`; `POST /api/reactions` populates it on insert from the existing parent-lookup query (no extra query). The follow-up NOT NULL tightening lands in v0.10.5 to keep the migration sequence safe (current prod has no `eventId` column at all; tightening alongside the column add would fail `prisma db push` since `ADD COLUMN ... NOT NULL` requires DEFAULT or empty table). `REPLICA IDENTITY FULL` set on `SetlistItemReaction` + `EventImpression` so DELETE/UPDATE pushes carry the full old row — needed to identify which `reactionCounts` cell to decrement and which impression id to remove on supersede. Optimistic-UI / push collision suppression NOT added: `<ReactionButtons>`'s `pendingPollCounts` stash + POST-response-authoritative pattern, and `<EventImpressions>`'s `mergeImpressions` chain-dedupe, already prevent the double-fire — adding a suppression Map would be belt-and-suspenders. CR-driven follow-ups: chain-aware merge in realtime `onUpsert` (id-based `mergeImpressions` left both old + new versions visible on supersede edits — fixed by extracting `mergeImpressionByChain` helper); `totalCount` sync in realtime callbacks so the "see older (X more)" math doesn't drift when other users post / hide / delete; `select: { id: true }` defensive narrowing on reactions create/findFirst (BigInt-serialization-risk hardening); circular dependency fix moving `Impression` type from `@/components/EventImpressions` to `@/lib/types/impression`.
- **#317 — R3: polling fallback + Sentry observability.** Production safety net: a Realtime channel that exhausts its retry budget hands off to the proven 5s/30s polling path within one render, invisibly to the audience. `pollFallback` state machine in both hooks — `CHANNEL_ERROR` or `TIMED_OUT` flips it permanently for the page lifetime (no auto-recovery, climbing back is too easy to flap during a live show; user can refresh to retry cleanly). `useSetlistPolling` is always called inside `useRealtimeEventChannel` (gated by `enabled && pollFallback`) so the fallback is wired and ready — flipping the flag swaps in polling within one render. `useRealtimeImpressions` exposes `pollFallback` in the return shape (consumer-controlled fallback, since the impressions feed's pagination state lives in the consumer). Reconnect-`SUBSCRIBED` after a transient drop refetches `/api/setlist` to fill any pushes that landed during the gap; first `SUBSCRIBED` after mount is the normal initial join (no refetch — already seeded), discriminated via a channel-bound ref. Warmup overlay: during the ≤5s window after fallback before the first poll lands, the return shape serves realtime's last-known state so the user doesn't see a flash of stale SSR `initialItems`. **Sentry**: breadcrumb on every status transition (separate categories `realtime` vs `realtime-impressions` so post-mortems can tell channels apart at a glance; level info for SUBSCRIBED, warning otherwise). Single `captureMessage` on first fallback per session per channel, latched via ref to avoid one report per status flip during sustained outages — tags carry `eventId` + `transitionReason` (CHANNEL_ERROR vs TIMED_OUT). Operator kill switch: `LAUNCH_FLAGS.realtimeEnabled = false` from launchFlags.ts forces all viewers back onto polling instantly. CR-driven: channel-bound refs (`hasSubscribedBeforeRef`, `hasReportedFallbackRef`, `hasReportedFallbackRef`) reset INSIDE the channel-setup effect, not in a sibling — locale changes also re-create the channel and need fresh refs; otherwise a redundant refetch fires on the new channel's first SUBSCRIBED. `useRealtimeImpressions` resets `lastUpdated` on eventId change so consumers don't briefly read the previous event's timestamp.
- **#319 — `<ActualSetlist>` auto-promote ticker (Realtime smoke regression fix).** `getConfirmStatus`'s 1-min auto-promote relied implicitly on `useSetlistPolling`'s 5s cadence as a re-render trigger — every poll forced a re-render, the function re-evaluated `new Date()`, and rumoured rows past `createdAt + 60s` settled within ≤5s of the boundary. The Realtime push path disconnects re-renders from any fixed cadence: pushes fire on actual data changes only, so a rumoured row stayed visually rumoured forever unless either a push arrived or the user reloaded. Smoke-test on dev caught it (Event 3 ongoing, `realtimeEnabled` flipped). Fix: explicit `setInterval(tickAutoPromote, AUTO_CONFIRM_TICK_MS)` in `<ActualSetlist>`, gated on `hasRumoured` (computed inline so the post-tick render re-derives — once every row settles, the gate flips false, the useEffect cleanup runs, and the ticker stops). Cost is zero for completed events and for sessions where every row is already confirmed; ≤5s of the boundary for live shows. Works uniformly under polling, realtime push, and R3 polling-fallback. New `AUTO_CONFIRM_TICK_MS` constant exported from `confirmStatus.ts` co-located with `AUTO_CONFIRM_MS` (boundary + worst-case latency intentionally paired). Bug only manifested with Realtime flag on, so not user-impacting today — but a hard pre-requisite for the activation PR.
- **#321 — Realtime status-boundary scheduler (sibling of #319).** Same root cause as #319 but downstream: `LiveEventLayout`'s `effectiveStatus = polledStatus ?? status` precedence let a stale `polledStatus` mask a fresh SSR `status` (refreshed by `<EventStatusTicker>`'s `router.refresh()` at startTime). Pre-Realtime, `polledStatus` refreshed every 5s from `/api/setlist` responses. With Realtime, only on push (Path B for SetlistItem and SongWish), so a startTime crossing in a no-activity window left polledStatus at "upcoming" indefinitely — wishlist editor stayed unlocked past startTime until the next unrelated push landed. Fix: extract `nextEventStatusBoundaryDelay` helper into `src/lib/eventStatus.ts` (next to `ONGOING_BUFFER_MS` and `POST_BOUNDARY_BUFFER_MS`); two consumers — `<EventStatusTicker>` (router.refresh on SSR side) and `useRealtimeEventChannel` (fetchSnapshot on Realtime side) — schedule at the same boundary instant so SSR + polled status stay in lockstep. New `startTime` option on `useRealtimeEventChannel` (string ISO; the caller in `<LiveEventLayout>` coerces with a Date-validity guard against `RangeError` from invalid Dates). Self-rescheduling setTimeout chain catches both `upcoming → ongoing` and `ongoing → completed` boundaries across one mount. CR-driven: `ONGOING_BUFFER_MS` imported in tests instead of redeclared (single source of truth); single-shot design comment added to `<EventStatusTicker>` to head off "why doesn't the completed flip refresh?" reviewer confusion.
- **#318 — `<UpcomingCard>` wish-open chip dropped to its own row.** Independent fix (caught between R3 and the smoke test). The 200px mobile scroll card on the home page squeezed the date when the D-day chip held the longer live-countdown label like "21 hours left" + the `🌸 예상 오픈` wish-open chip both fit on the same row as the date. Restructured the layout so the wish-open chip occupies its own row when present, leaving the date row uncrowded. CSS-only; no logic changes.
- **Tests: 815 → 818 (+3 across the auto-promote ticker, +0 net from the architecture itself since the realtime hook had its tests added with R3).** R1 verified end-to-end with type-check + lint + build + 804 tests at landing time; R2 added 0 net tests (existing reactions-route tests updated for the new `eventId` populate); R3 added 11 new fallback tests (6 for `useRealtimeEventChannel`, 5 for `useRealtimeImpressions`); #319 added 3 ticker-gating tests; #321 added 4 boundary-scheduler tests on top of the existing 7 EventStatusTicker tests (still pass — helper extraction is byte-equivalent). Note: `slug.test.ts` shows 2 transient flakes under parallel test load (kuroshiro JA-analyzer cold-start timeout) — pre-existing, both pass in isolation.
- **Operations footnote — Supabase Realtime infra + new env vars + DB schema additions.** Three operator-side prep steps before the activation PR can ship: (1) add `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` to Vercel Production environment variables; (2) verify the Realtime extension / `supabase_realtime` publication exists on the prod Supabase project (`SELECT * FROM pg_publication WHERE pubname = 'supabase_realtime'`); (3) wait for `migrate-prod.yml` to run on the v0.10.4 tag — `prisma db push` adds `SetlistItemReaction.eventId` (nullable in this release; v0.10.5 tightens to NOT NULL after the backfill verifies clean), `prisma db execute --file prisma/post-deploy.sql` runs the backfill + adds the four tables (SetlistItem, SetlistItemReaction, EventImpression, SongWish) to the publication + sets REPLICA IDENTITY FULL on the two diff-merge tables. The Realtime infra carries non-trivial WAL overhead per change (REPLICA IDENTITY FULL widens DELETE/UPDATE payloads to the full old row), but only on the four published tables and only on changes — idle cost is zero. Activation PR (separate, planned as v0.11.0 since it's a meaningful behavior shift) flips `LAUNCH_FLAGS.realtimeEnabled = true` and deletes the polling-path branches in `LiveEventLayout` + `EventImpressions` (the polling hooks themselves stay alive as the in-hook R3 fallback path).

### v0.10.3 (2026-05-09)
- **Phase 1B GA4 custom events — eleven fire-and-forget events instrument the wishlist + predict + share-card + confirm + disagree + home surfaces shipped across v0.10.0–v0.10.2.** All events go through the existing `src/lib/analytics.ts` `trackEvent` helper unchanged; no new GA4 dimensions, no new identifiers (no `userId` / `anonId` / `sessionId` / IP hash to GA4 — Phase 1A rule preserved), no new runtime deps, no schema changes. Single-PR release, lands ahead of the 5/16 user-open and the 5/23 Kobe gated-Confirm UI test where `confirm_click` + `disagree_click` are the only signal the test produces (`LAUNCH_FLAGS.confirmDbEnabled = false` until 5/30).
- **#312 — Phase 1B GA4 custom events.** Eleven events across six surfaces:
  - Wishlist (`<EventWishSection>`): `wish_add` / `wish_remove` fire only after server POST/DELETE 2xx, not on optimistic UI mutation — PR #298's polling-race fix means wish state can re-derive across remounts, so the trackEvent calls live inside the user-action callbacks (not in `useEffect` chains that re-derive from polling) so a polling refresh can never spam the event. `song_id` reads from `entry.songId` (the DB Song.id), never `entry.dbId` (the EventWish row id) — KPI groups by song.
  - Predicted Setlist (`<PredictedSetlist>`): `predict_add` after `writePredictions` commits to localStorage; `predict_reorder` once per `onDragEnd` drop commit, never `onDragMove`, no-op drops (`active.id === over.id` or index-not-found) skipped; `predict_lock_view` deduped per session per event via `sessionStorage` key `predict_lock_viewed:{eventId}` (not `localStorage` — the count must reset across sessions to compute "% of returning visitors who view live results during the show"). The lock-view effect mounts adjacent to the existing `markLocked` effect so the next polling-driven re-render past `startMs` fires it once. No `predict_remove` event — prediction is unranked locally and users edit until lock; remove signal is low-value.
  - Share Card (`<ShareCardButton>` + `<ShareCardModal>`): `share_card_open` fires only on enabled-state opener taps (the disabled-during-show variant from #307 stays a no-op). `share_card_save` covers both download + native-share success with `outcome: 'downloaded' | 'shared'` so PR #306's mobile OS-sheet path and the desktop download fallback both feed one funnel — single GA4 query for total saves, outcome param breaks down by surface. **`cancelled` (user dismissed the OS share sheet) fires NO event** — matches the modal's no-toast UX, and tracking it would inflate the share-funnel denominator with non-actions. `share_card_save_failed` carries a `reason` enum (`'timeout' | 'tainted_canvas' | 'oom' | 'unknown'`) classified at the call site by inspecting `outcome.message` from the existing `ShareOutcome` shape — no restructuring of `src/lib/shareCard.ts` to return structured codes (helper untouched, classification table is one ~6-line function `classifyShareCardFailure` at the top of the modal). The defensive `try/catch` around `shareCard()` also fires `share_card_save_failed` with `reason: 'unknown'` so a future rethrow path doesn't silently swallow the failure.
  - Confirm + Disagree (`<ActualSetlist>`, NumberSlot consumer): **🔴 5/23 Kobe blocking subset.** `confirm_click` fires on 👍 tap **before** `useLocalConfirm.toggleConfirm` (which contains the gated POST), with `target_state: 'to_confirmed' | 'to_unconfirmed'` and `db_enabled: LAUNCH_FLAGS.confirmDbEnabled` carried as a per-event param (not a session-scoped custom dim) so the 5/30 cutover stays queryable in a single GA4 view across the boundary — half the events will carry `db_enabled: false` (Kobe), the other half `true` (Kanagawa onward). `disagree_click` fires on 👎 tap before the local toggle; no `db_enabled` param since `useLocalDisagree` is local-only at v0.10.x (Week 3 brings the schema). The 1-min auto-promote in `getConfirmStatus` is NOT a tap — auto-promotion lives in `deriveRowState` and never reaches the click handlers, so it correctly does not emit `confirm_click`.
  - Home Upcoming Card (`<UpcomingCard>`): `home_upcoming_click` carries `badge_visible` reusing the same `showWishBadge` boolean that drives the 🌸 badge render — A/B-style attribution for the D-7 badge UX investment without a separate `home_card_badge_click` event (the boolean param subsumes the question). Threaded a new explicit `eventId: string` prop from `src/app/[locale]/page.tsx` through to the card rather than parsing it from `href` at runtime — cheaper, type-safe, matches the project's "stringify before GA4" discipline (BigInt would throw on serialization).
  - **Reconciliations vs the predecessor task spec.** PR #306's mobile native-share return (landed three hours before the analytics PR) reintroduced `navigator.share` after #295 had collapsed the flow to download-only — the spec was written assuming download-only and contemplated only `'downloaded'` + error outcomes. Plan adapted: `share_card_save` carries the `outcome` discriminator instead of forking into a separate `share_card_share` event, keeping the count at 11. The spec also described `<ConfirmButton>` and `<ThumbsVote>` as separate components with a unified `vote_click` (`vote: 'up' | 'down'`); on dev the 👍 IS the confirm action (`useLocalConfirm`) and the 👎 is the disagree (`useLocalDisagree`) — both unified into `<NumberSlot>` by PR #293. Plan adapted: `vote_click` dropped entirely, replaced by `disagree_click` for the 👎 surface only; the 👍 fires `confirm_click` only (which already carries richer `target_state` + `db_enabled` params), avoiding double-counting and making the event names match the underlying hooks.
  - **Out of scope (deliberate omissions).** No `setlist_tab_change` event — default GA4 `page_view` covers tab state via URL hash on the same page, low-signal for Phase 1B KPIs. No server-side analytics forwarding (Measurement Protocol). No new GA4 dimensions / audiences / property config. No tracking of un-share-card paths killed by the v0.10.x rewrites (`navigator.share` text-only, Twitter intent — they don't exist anymore). No D-3/D-1 reminder-tweet inflow attribution — covered by GA4 default UTM Source/Medium.
- **Tests: 804 → 804 (unchanged).** GA4 calls are third-party fire-and-forget; the project's existing testing-safety-net pattern is "verify in Realtime, not in mocks" — same convention Phase 1A's `task-analytics-tracking.md` (predecessor) followed. Verification happens on the Vercel Preview against GA4 Realtime reports per the `wiki/output/task-phase1b-analytics-tracking.md` §"Verification" matrix.
- **Operations footnote — no v0.10.x DB or env changes.** No schema migration, no new env vars, no new runtime deps. The 5/30 Kanagawa Day-1 Confirm DB write activation remains a separate one-line commit (drop `LAUNCH_FLAGS.confirmDbEnabled`); the `db_enabled` per-event param means the GA4 query for confirm-tap intent stays consistent across that flag flip.

### v0.10.2 (2026-05-09)
- **Phase 1B Stage C polish — nine PRs address operator + user-test findings on the wishlist + predict + share-card surfaces from v0.10.0/v0.10.1, plus an upstream Next + React security bump.** All UX swaps + i18n improvements + one dependency patch; no schema migration, no new env vars, no new runtime deps. The share-card surface gets the bulk of the work: downloaded-image glyph clipping, OS share-sheet on mobile, and the during-show "the button exists, just not yet" hint.
- **#308 — `next 16.2.2 → 16.2.6` + `react 19.2.4 → 19.2.6` (security).** Routine upstream patch bump — both Next and React shipped security fixes in the matching patch versions. Lockfile-only churn elsewhere, no API changes affecting our code.
- **#306 — Share Card: native OS share sheet on mobile, download elsewhere.** v0.10.1 (#295) collapsed the share flow to download-only after the Twitter web-intent broke. This re-introduces `navigator.share({ files })` with a different framing — generic OS sheet, no specific app promised. Capability-gated on `navigator.canShare({ files: [pngFile] })`: when true (mobile, recent Safari + Chromium) the OS sheet surfaces Photos / Messages / KakaoTalk / Twitter / Discord / etc.; when false (most desktop browsers) the existing download path runs unchanged. New outcomes `shared` / `cancelled` are silent (the OS already gave the user feedback); non-abort share failures fall through to the download fallback so the user always ends up with the image. `shareCard()` grows an optional `share: { title, text, url }` option; `<ShareCardModal>` passes the event title + a score-summary text + canonical URL so platforms that compose a body alongside the file (Messages / email) get a useful default. Same PR drops the **"Copy link" sibling CTA** — its label implied a per-image URL, but the share card is rendered client-side and never hosted, so the button only ever copied the event URL (under-delivering on its implied promise). `shareUrl` prop stays since `navigator.share` consumes it; `copyLink` / `linkCopiedToast` / `linkCopyFailedToast` i18n keys deleted in all three locales. New `shareText` i18n key carries the score summary. Test suite regrown from "asserts navigator.share is NEVER called" (the v0.10.1 regression guard) into positive coverage for share / cancel / share-failure-falls-back-to-download / canShare-false paths.
- **#307 — Share button visible during the show, disabled with a hint.** v0.10.0/v0.10.1 mounted the share button only after `status === completed`, so a fan watching the live tab during a show had no way to know the share affordance even existed — it just popped in at completion. Show the button during ongoing events too, but disabled, with the existing `shareDisabled` i18n hint ("공연 종료 후 활성화됩니다" / "Available after the show ends" / "公演終了後に有効になります") to its left. Visibility now keys on `predictions.length > 0` for both ongoing + completed; enabled only when `status === completed && actualSongs.length > 0 && predictions.length > 0`. Disabled styling reuses the modal's busy-state slate gray (CR-driven: routed through `colors.textMuted` instead of a hardcoded `#94a3b8`). Pre-show + zero-prediction cases still return null. Modal is gated on `enabled` at render time so html2canvas + modal state aren't paid for in the disabled path. Regression-guard tests expanded from 4 to 5 cases covering all five state combinations explicitly.
- **#305 — Share Card downloaded image: glyph-clipping fix.** Operator screenshot showed every song title in the saved PNG cropped at top + bottom. html2canvas reads the *computed* style strictly: each title span had `whiteSpace:nowrap + overflow:hidden + textOverflow:ellipsis` and **no explicit `lineHeight`**, so the line-box collapsed to roughly the font size and html2canvas captured at that height. Browser preview was forgiving (extra glyph extent painted slightly outside the box but didn't visually clip on screen) but the saved image hit the strict-render path. Setting `lineHeight: 1.5` reserves ~20 px of vertical room per row — comfortably above any Latin descender (`g`/`p`) or CJK glyph extent. Live preview reads the same as before.
- **#303 — Wishlist my-list column highlights matched songs during/after the event.** The right column (모두의 희망곡) wrapped each row in `<SongMatchBadge>` so once the event starts and the actual setlist begins to fill, any top-3 entry that matches an actual song lights up green. The left column (나의 희망곡) was missing the same wrapper, so a fan whose own pick got played got no visible confirmation in their wishlist — they had to scroll down to the live setlist to know their wish "hit". Mirrored the right-column treatment exactly, including the `disabled={!isLocked}` gate so pre-show stays read-only and candidate-flavored. Added `min-w-0` to the row span so the badge's truncation rules collaborate with the parent's `truncate` instead of fighting it.
- **#302 — SongSearch dropdown shows the artist's full name (not the short name).** The dropdown was rendering each result's artist with `displayNameWithFallback(..., "short")`, which collapsed groups with a curated `originalShortName` (e.g. 蓮ノ空 sub-units) to the abbreviation. Search results need the unambiguous full name so the user can tell similarly-named artists apart at a glance — switched the mode argument to `"full"`. The route already projects both `name` and `shortName`, so no API change needed; existing test fixture uses `originalShortName: null` so both modes resolve identically and tests stay green.
- **#301 — Wishlist box title + subtitles + add-button: phase-aware vocabulary, 희망곡 family throughout.** Pre-show title "희망곡" / "Wishlist" / "リクエスト" was too generic — a first-time visitor couldn't tell what the box invited them to do. The component already toggled `title` ↔ `lockedTitle` based on `isLocked`, so this just redirects the two existing keys to phase-appropriate phrasings: pre-show invitation (`title`: 이번 공연에서 듣고 싶은 곡 / Songs you want to hear at this show / この公演で聴きたい曲) and during/after collective (`lockedTitle`: 모두의 희망곡 / Everyone's wishlist / みんなの希望曲). Dropped the trailing " TOP3" from `lockedTitle` since the medals (🥇🥈🥉) + "Up to 3" cap hint already convey the ranking. Column subtitles renamed to mirror the title vocabulary: `myList` 내 선택 → 나의 희망곡 (My wishlist / 私の希望曲); `fanTop3` 팬 TOP3 → 모두의 희망곡 (Everyone's wishlist / みんなの希望曲) — when locked, the right-column subtitle reads identically to the section title; medals still differentiate visually. Add-button label aligned with Predict's "곡 추가" / "Add a song" / "曲を追加" so the two song-picking surfaces (wishlist + setlist prediction) stay verbally consistent. Japanese **リクエスト → 希望曲** in `lockedTitle` + `removeAria` — リクエスト literally means "request" (asking the artist to play it), which doesn't fit a "songs I hope to hear" surface; 希望曲 is the natural fan-culture equivalent of Korean 희망곡 and lets the JA/KO namespaces parallel each other one-for-one.
- **#300 — Home wish-open badge: rephrase for clarity.** "예상 오픈" / "Predict open" / "予想公開" read like a release/store-opening notice, not what it actually meant ("the setlist-prediction window for this event has just opened"). Rephrased to lead with the noun the badge is actually about: 세트리스트 예상 가능 / Setlist Prediction Available / セットリスト予想受付中. Refreshed the JSDoc comments in `UpcomingCard` / `page.tsx` and the test comment in `setlist-section.test.tsx` that quoted the old badge text so a future grep for the badge name still lands on these references.
- **#299 — Upcoming card date no longer wraps to two lines on the 200 px mobile scroll card.** The first home-page upcoming card's date ("Sun, May 10") was wrapping to two lines whenever the D-day chip held the longer live-countdown label like "21 hours left" — flex `gap-2` between the badge and the date span squeezed the date into too little room. Added `whitespace-nowrap` to the formatted-date span and to both chips (D-day + wish-open) so the row stays single-line; flex `justify-between` lets the chip cluster shrink first if width runs out. Conflict-resolved during merge with the wish-badge wrapper from #300.
- **Tests: 800 → 804 (+4 across the nine PRs).** Mostly i18n + visual changes, so most PRs added zero tests. Notable additions: native-share happy path + AbortError-cancel path + share-failure-falls-back-to-download + canShare-false-skips-share-path (ShareCard); five-case display-gate matrix for ShareCardButton (renders disabled+hint during ongoing, enabled when all gates pass, hides in upcoming/zero-predictions/zero-actuals).
- **Operations footnote — no DB or env changes.** No schema migration, no new env vars, no new runtime deps beyond the Next/React patch bump. The 5/30 Kanagawa Day-1 Confirm DB write activation remains a separate one-line commit (drop `LAUNCH_FLAGS.confirmDbEnabled`) per v0.10.x.

### v0.10.1 (2026-05-08)
- **Phase 1B Week 2 polish — eight PRs address smoke-test findings on top of v0.10.0.** All bug fixes / UX swaps; no schema migration, no new env vars, no new runtime deps. Most issues caught by operator smoke between the v0.10.0 tag (5/7) and the 5/16 user-open window.
- **#287 — Wishlist D-7 gate is pre-show-only.** v0.10.0 hid the wishlist section entirely once `isWishPredictOpen` returned false — including ongoing/completed events — so the during/post-show display rules (TOP-3 + match-highlights) couldn't render. Split the visibility decision: D-7 governs the pre-show editor; ongoing/completed always render. The home page badge keeps using `isWishPredictOpen` as-is; the event detail page now ORs it with `status !== "upcoming"`. Operator caught: prediction tab worked during the show but the wishlist column went blank.
- **#288 — Home `🌸 예상 오픈` badge on D-0 (same UTC day, pre-startTime).** v0.10.0's `shouldShowWishBadge(daysUntil)` required `daysUntil > 0` on the rationale that "D-0 is about to flip to ongoing via the auto-status ticker." Wrong — the ticker fires at `now >= startTime`, not when `daysUntilUTC` drops to 0. There's a window of up to ~24 h on the event's UTC day where the event hasn't started yet but `daysUntil === 0`, exactly when fans want the predict badge highlighted. Smoke caught: a 4 h-before-start view dropped the badge while a 12 h-before-start view kept it (the 12 h sample sat across the UTC midnight boundary). Drop the strict `> 0` to `>= 0`; aligns the home badge with `isWishPredictOpen` (which already treated D-0 as inclusive).
- **#289 — Trending hide pre-show; hide when empty post-show.** Two related operator preferences: (a) the Trending box visible pre-show was a confusing affordance (no reactions yet, an empty 0-count nudge); (b) post-show with zero reactions, the empty-state nudge took space without value. Render condition tightened to `status !== "upcoming" && trendingSongs.length > 0`. The component itself unchanged — only its render gate.
- **#290 — `<SongSearch>` portal escapes ancestor `overflow:hidden`.** Wishlist + predicted-setlist surfaces both wrap `<SongSearch>` in a card with `overflow: hidden + border-radius`. The dropdown's `position: absolute` rendered as a descendant of the input's container, so the clipping ancestor cropped it to whatever vertical space remained inside the card — about one row on the wishlist's compact input. Arrow-key nav still moved through hidden items but only row 1 was visually reachable. Admin's setlist song search wasn't affected (no clipping ancestor). Render the dropdown via `createPortal` to `document.body` with `position: fixed` coords computed from the input's `getBoundingClientRect`, repositioning on `scroll(capture)` + `resize` + `visualViewport` events (the last for iOS Safari keyboard show/hide). Click-outside detection updated to consult both refs (input container + portalled dropdown). New `zIndex.dropdown = 100` token between sticky (10) and modal (200) so a future picker inside `<ShareCardModal>` etc. stays under its host dialog. Two regression tests pin: listbox renders into `document.body` (not host card), and clicking a portalled option still fires `onSelect` (the click-outside handler doesn't pre-empt).
- **#291 — Wishlist + predicted-setlist lock past `startTime`: server 403 + wall-clock fallback + remount + tab guard.** v0.10.0 relied on a single `setTimeout` flipping `scheduledLocked` at `startMs` to lock the editors. Operator caught: opening the page pre-show and leaving it across the start time kept both editors fully editable — laptop sleep / mobile lock pauses JS timers, so the OS clock advanced but the browser's timer didn't. Three layers of defense added: (1) server-side `POST /api/events/[id]/wishes` 403s when `now >= event.startTime` so a leaked editor still can't write; (2) wall-clock check `mounted && Date.now() >= startMs` re-derived at every render so the next polling-driven re-render past `startMs` catches missed setTimeouts (block-form `eslint-disable react-hooks/purity` because the violating call sits on line 4 of the multi-line expression); (3) `key={eventId}` on `<EventWishSection>` and `<PredictedSetlist>` forces remount on cross-event navigation so `scheduledLocked` state can't leak between events. CR caught a sibling correctness gap on the same PR: even with `key={eventId}`, `<SetlistSection>`'s own `activeTab` state persisted across event nav — guarded `renderedTab` with a three-branch fallback that forces "actual" when `!hasPredictions`. Reactions-on-rumoured policy ([[conflicts#8]]) preserved.
- **#293 — Replace `<FlagButton>` with thumb-up / thumb-down vote buttons.** v0.10.0 shipped a `<FlagButton>` on rumoured rows that opened a `mailto:help@opensetlist.com` link prefilled with event id, position, and song title. Two problems: (a) email is too demanding for casual users — tap → mail picker → compose → send → operator triages by hand, friction beyond what the operator-expected report volume justifies; (b) the original conflict-resolution intent was thumbs voting (Week 3 `<AddItemBottomSheet>` ships the conflicting-item entry path; viewers should vote on which is correct so the right one auto-promotes — `mailto:` is a one-way escalation channel, not a vote channel). Replaces `[?]` / `[✓]` with side-by-side `[👍][👎]` 22×22 buttons. The 👍 keeps the existing confirm semantics (one closer to the auto-promote threshold). The 👎 is a new lightweight "wrong" signal — tap-to-disagree, localStorage-tracked (`disagree-{eventId}` key), no email. Mutual exclusivity coordinated in `<ActualSetlist>`'s tap handlers. New `useLocalDisagree` hook mirrors `useLocalConfirm` (no POST yet; disagree aggregation deferred to Week 3 alongside the schema for `SetlistItemDisagree`). New `colors.disagreeBg` / `colors.disagreeText` rose tokens. Position column widens 36 → 52 px on both mobile and desktop. CR-driven: buttons get `disabled` + `aria-disabled` when a tap handler isn't passed, since admin/preview consumers may render rows without vote actions.
- **#294 — Predict + wish lock: server-resolved status closes client-clock-skew bypass.** #291's setTimeout + wall-clock layers both rely on client time. Operator confirmation: changing device time is too easy at this scale, so a user with a slow device clock could keep the editors open past server-side startTime. Server-authoritative `status` added as a third lock input — `/api/setlist` now runs a 4th parallel query for `event.{status, startTime}`, computes `resolvedStatus` via the same `getEventStatus` helper the page uses at SSR, returns it alongside the existing setlist payload. `useSetlistPolling` exposes the polled status to consumers; `<LiveEventLayout>` derives `effectiveStatus = polledStatus ?? initialStatus` and threads it into both `<EventWishSection>` (new prop) and `<PredictedSetlist>` (already had a status prop). The OR-chain becomes `scheduledLocked || status !== "upcoming" || (mounted && Date.now() >= startMs)`. Predicted setlist has no DB persistence at Phase 1B, so this is the strongest gate possible without moving predictions to DB (deferred to Phase 2 leaderboard work). Note: the original PR #292 was auto-closed by GitHub when its base branch (`fix/wishlist-lock-server-and-fallback`, the #291 head) was deleted post-merge — GitHub's stacked-PR auto-retarget didn't fire because the deletion came via raw git protocol rather than the post-merge UI button. Re-opened as #294 with the same head retargeted to `dev`.
- **#295 — Share Card simplified to download-only "Save image".** v0.10.0 had two paths: mobile `navigator.share({ files })` + desktop `<a download>` then `window.open(twitterUrl)`. Operator-reported: tapping "트위터에 공유" opens Twitter compose with no image (the silent download + toast nudge happens but the auto-opened tab grabs focus and users conclude the button is broken); on mobile the share sheet sometimes doesn't even include Twitter (depends on installed apps and active share extensions). Twitter's web intent is text+URL only — no API to attach an image via URL params, no other public web endpoint for prefilling a tweet with media. Drop both branches. Single platform-agnostic flow: html2canvas → blob → `<a download>` → toast `"이미지가 저장됐어요"`. User shares the saved file manually wherever they want (Twitter web compose, KakaoTalk, Discord, Photos). `<ShareCardModal>` button label `shareTwitter` → `saveImage`, primary color from Twitter-blue to brand gradient. `ShareOutcome` collapses from 5 variants to `downloaded | error`; `ShareCardOptions` drops `text` / `url` (no longer consumed). New `imageErrorToast` surfaces tainted-canvas / OOM failures so users get feedback on rare-but-real errors. `handleShare` wrapped in `try/catch/finally` so an unexpected throw can never lock the modal in `busy=true`. Regression test asserts neither `navigator.share` nor `window.open` is called. The "Copy link" sibling CTA stays as-is.
- **Tests: 763 → 800 (+37 across the eight PRs).** Highlights: useLocalDisagree hydration + scoping + malformed-JSON tolerance; eventTiming D-0 inclusive boundary regression; share-card download-only happy-path + filename override + regression guard ("does NOT call navigator.share or window.open") + toBlob-null error path; SongSearch portal listbox-renders-into-body + click-outside-vs-option-click race; event-wish-section server-status-lock cross-clock-skew scenario + tightened unlocked-title regex.
- **Operations footnote — no v0.10.x DB or env changes.** No schema migration, no new env vars, no new runtime deps. The 5/30 Kanagawa Day-1 Confirm DB write activation remains a separate one-line commit (drop `LAUNCH_FLAGS.confirmDbEnabled`).

### v0.10.0 (2026-05-06)
- **Phase 1B Week 2 — six PRs ship the wishlist + prediction + share-card + D-7 + Confirm-UI surfaces in a coordinated drop.** The 5/16 user-open scope is now feature-complete in dev; this is the deploy-readiness release. Per-PR detail below.
- **#278 — schema: `SongWish` + `SetlistItemConfirm` (Stage A foundation).** Two additive Phase 1B/1C tables. `SongWish` is `(eventId, songId, anonId)`-keyed with a partial-unique on `(eventId, songId, anonId) WHERE anonId IS NOT NULL` to prevent the same fan from submitting the same song twice; `SetlistItemConfirm` is `(setlistItemId)`-only — no `anonId`, no partial unique — because Phase 1C's confirm aggregation deliberately accumulates raw confirm-events and lets Week 3's threshold-aggregation handle dedup ([[crowdsourcing#9]] schema simplification rationale). `LAUNCH_FLAGS.confirmDbEnabled = false` placed in `src/lib/launchFlags.ts` with a docstring noting that the entry's removal IS the 5/30 Kanagawa Day-1 activation. Schema-only ship; UI/API consumers all later in the chain.
- **#279 — Phase 1B 희망곡 (wishlist) feature.** Pre-show fans pick up to 3 songs they want to hear; during/post-show the fan TOP-3 surfaces alongside match-highlights against the actual setlist. `<EventWishSection>` mounts above Trending in the right column with a mobile 2-col / desktop 2-row layout per the mockup. Storage is **localStorage `wish-{eventId}` + DB hybrid**: localStorage owns dedup pre-POST and the cap-of-3 enforcement, the `dbId` returned from POST is stored locally so DELETE on undo can target the right row, the DB write enables the polled fan TOP-3 aggregate. Phase 1A `anonId` cookie is reused for server-side dedup (no IP-hash — same lesson as Phase 1A reactions, mobile cellular NAT broke it). New `<SongMatchBadge>` reuses `isSongMatched` from `src/lib/songMatch.ts` with variant + medley constituent matching (predicted "Dream Believers" hits "Dream Believers (105th Ver.)"; predicted ハナムスビ hits a medley containing it) — same primitive Predicted Setlist consumes downstream. SSR seed for fan TOP-3; polled refresh threaded through `/api/setlist` (not a separate channel) so the existing 5s cadence drives it. Pre-show editor + post-lock TOP-3-only display, gated by `event.startTime` UTC. CR caught a shared-mutable-`[]` regression (`readWishes` had been returning a single frozen empty default that mutated across consumers via `.push()`) — fixed in the same round. New `wishlistFocusBorder` token added so the picker's compact-input focus ring can drift independently of the brand-blue palette.
- **#280 — `<SetlistSection>` tab refactor + row-state scaffold (Stage B).** Pure refactor — Phase 1A events without predictions render byte-equivalent. New `<SetlistSection>` owns tab visibility per the matrix in `wiki/output/task-week2-setlistsection-tab-refactor.md`; new `<NumberSlot>` carries `confirmed | rumoured | my-confirmed` visual states (Stage C wires the actual `onTap` handler). Three-state row visuals: confirmed renders as a plain right-aligned number (byte-equiv span); rumoured shows a `[?]` dotted-border 22×22 button + gray row bg; my-confirmed shows a `[✓]` solid sky-blue 22×22 button + gray row bg. WAI-ARIA tabs: paired tab/panel ids via `useId`, roving `tabIndex` (active=0, inactive=-1), `ArrowLeft/Right` cycling with wrap + Home/End jump, `requestAnimationFrame`-deferred `.focus()` on activation so subsequent Tab keys exit the tablist (without the explicit focus-move, the user's keyboard focus stays on the now-inactive tab and Tab navigation traps). Tabs sit BELOW the existing `SETLIST h2 + LIVE pill + count subtitle` — operator preference, deviation from the mockup, preserves the LIVE indicator in all states. CR caught two regressions on the same PR before merge: `role="tabpanel"` was advertised on the no-tabs path (orphaned `aria-labelledby` pointing at a non-existent tab id) and the `emptyFallback` short-circuit in `<LiveSetlist>` would have starved Stage C's predictions-only-no-actual case (case 1 in the visibility matrix) the day Stage C lands; both fixed in the same round.
- **#281 — Phase 1B Stage C: Predicted Setlist + Share Card.** Real `<PredictedSetlist>` body fills the Predicted tab — pre-show / during-show / post-show modes with `predict-{eventId}` localStorage writer, drag-reorder via `@dnd-kit/sortable` (5 px activation distance + KeyboardSensor for a11y), inline `<SongSearch variant="compact">` add row, lock at `startTime` UTC. Score functions in `src/lib/predictScore.ts`: `calcPredictScore` is the position-rank rule that drives the during-show divider (`현재 N곡 — 여기까지 매칭` amber bar between in-rank and below-divider rows; out-of-rank matches dim to opacity 0.4 and auto-promote when actual count grows past their rank); `calcShareCardScore` is the order-independent variant for the post-show share card (every match counts equally, distinct from the live-tab rule per task spec §22 — explicitly documented to prevent over-unification). `pendingSongs` is `Set`-deduped before return so a single prediction matching multiple actual rows (medley constituent appearing twice; encore reprise) doesn't push the same songId multiple times into the dimmed display. Share card surface: `<ShareCardButton>` (post-show only) opens `<ShareCardModal>` rendering `<ShareCardPreview>` (the html2canvas capture target). Two themes (`shareCardColors.dark` / `light` in tokens.ts — full color tokens per the spec, distinct from the brand palette so a future brand tweak doesn't repaint shared images already floating around social media). CTAs: `navigator.share` on mobile / download + Twitter intent on desktop, plus copy-link with `linkCopiedToast` / `linkCopyFailedToast` outcomes. `popup_blocked` outcome surfaces a distinct toast when `window.open` returns null (popup blocker intercept); `toBlob` 10s timeout prevents stuck-busy on tainted-canvas / OOM browser conditions. Predict-tab visibility relaxed to `storedHasPredictions || status === "upcoming"` — CR caught the original `storedHasPredictions`-only gate as a Major bug: a first-time visitor on an upcoming event with no localStorage would have dropped straight to the empty fallback with no path into the Predicted UI, shipping the 5/16 user-open feature dead. Three more CR rounds added ICU plurals to `messages/en.json` for `dividerLabel` / `preShowHint` / `duringHint`, fixed an `actualSongs` placeholder-row filter (`type === "song" && songs.length > 0` — without the second half, admin-created song-typed placeholders inflated the `total` denominator), restored modal focus to the opener button on close, and dropped a dead `<PredictScoreBadge>` component the prepush hook flagged (component built but never imported; the `<SetlistTabs predictedBadge>` slot stays as a defined optional prop for the eventual re-add when predictions state can be lifted to `<SetlistSection>` without going stale across mutations).
- **#282 — D-7 open-window gate + home page badge.** Wishlist + Predicted Setlist surfaces hide outside the 7 days before `event.startTime` per `raw/20260503-1b-1c-timeline.md` §"희망곡/예상곡 표시 조건"; pre-D-7 the surfaces are hidden entirely on the event detail page, and home page Upcoming cards within D-7..D-1 grow a `🌸 예상 오픈` badge + emphasized `colors.wishlistBorder` resting stroke. New `src/lib/eventTiming.ts` exports `WISH_PREDICT_OPEN_DAYS = 7`, `isWishPredictOpen({ startTime, status }, now)`, and `shouldShowWishBadge(daysUntil)`. The gate is computed server-side (page → `<LiveEventLayout>` → `<LiveSetlist>` → `{<EventWishSection>, <SetlistSection>}`) and threaded as a boolean prop; snap-frozen at SSR with the same `referenceNow` that drives `getEventStatus`, refresh handles the rare midnight-UTC boundary crossover (≥ 1-day session). D-0 is **inclusive** for `isWishPredictOpen` (event later today is still in the window) but **exclusive** for `shouldShowWishBadge` (the home card is about to flip to ongoing via the auto-status ticker, so showing `🌸 예상 오픈` on D-0 would be just noise). `utcDayStart` / `utcDayOffset` / `daysUntilUTC` relocated from inline `src/app/[locale]/page.tsx` into the new helper so the home-window query and the D-7 gate share one UTC-anchored definition (CLAUDE.md §"Date & Time" discipline; vanilla `Date.UTC()` math, no `date-fns` dep). For `<SetlistSection>` the predict-tab gate now splits into `status === "upcoming" ? isWishPredictOpen : storedHasPredictions` — pre-D-7 hides the tab even with stored predictions ("keep the data, just don't surface the tab" per spec; rare edge case if an event date moved backward after predictions were typed). CR caught a real correctness gap: the helper relied on upstream `getEventStatus` having narrowed `status === "upcoming"` to `now < startTime`, but as a standalone function it could still return true for an event whose `startTime` is earlier today (same UTC day, status auto-flip lag near `startMs`). Fixed with strict `start.getTime() > now.getTime()` guard and a regression test for the same-UTC-day-but-past case.
- **#283 — Phase 1C Confirm UI for 5/23 Kobe Day-1.** UI flow ships now; DB writes activate at 5/30 Kanagawa Day-1 by removing the `LAUNCH_FLAGS.confirmDbEnabled` entry. `<NumberSlot>` `[?]` / `[✓]` taps wire through new `useLocalConfirm` hook (per-viewer `confirm-{eventId}` localStorage; mounted-gated hydration matches the wish/predict pattern). `getConfirmStatus` pure helper handles the 1-min auto-promote: read-time `now` + 5s `useSetlistPolling` cadence means rumoured rows past `createdAt + 60s` settle within ≤ 5s of the boundary without an extra `setInterval`. The helper returns the binary confirmed/rumoured bucket only; the my-confirmed *visual* state is composed by callers from `confirmStatus === "rumoured" && localConfirmedIds.has(id)` — keeps the function a pure data-derivation testable without knowing about row visualization. `live` status maps to confirmed for visual purposes (matches Stage B `deriveRowState`); malformed `createdAt` and unknown statuses fall back defensively to confirmed. `<FlagButton>` is a small `mailto:help@opensetlist.com` link inline below the title for rumoured rows ONLY (not my-confirmed — flagging a self-endorsed row contradicts itself), with an i18n-templated subject + body containing event id, row position, and locale-resolved song title; Phase 2 swaps the destination for an internal endpoint when threshold-based reporting ships. POST `/api/setlist-items/[id]/confirm` always returns 200; flag-off path returns `{ ok: true, skipped: "feature_flag_disabled" }` so ops can monitor would-have-confirmed call rate during 5/23 Kobe testing without DB contamination. No `anonId` recorded ([[crowdsourcing#9]]); no partial-unique constraint — Week 3's threshold-aggregation handles dedup. `LiveSetlistItem.createdAt: string` newly exposed to TS (Prisma already returned it on default `findMany` include + `serializeBigInt` stringifies; this just makes it visible to type consumers). Reactions-on-rumoured policy ([[conflicts#8]]) preserved — the existing test at `src/__tests__/unit/setlist-row.test.tsx:716` covers the regression. CR caught a StrictMode-doubling concern: side effects (`writeStored`, gated `fetch`) inside the `setConfirmedItemIds` updater would have fired twice in dev once `confirmDbEnabled` flips true on 5/30, accumulating duplicate `SetlistItemConfirm` rows in the dev DB (production isn't affected — StrictMode is dev-only). Side effects moved to event-handler scope; `useRef` + `useEffect` mirror keeps the callback's view of the latest set fresh without taking `confirmedItemIds` as a dep, with an optimistic ref update inside the handler covering the rapid-tap window where the sync effect hasn't run yet. Render-phase ref mutation was the first attempt but the project's `react-hooks/refs` rule blocks that pattern; useEffect sync is the lint-clean equivalent.
- **Tests: 710 → 763 (+53 across the six PRs).** Dominant additions: predictScore (live + share-card score variants + auto-promotion + dedup edge cases); predictionsStorage (raw-parse `lockedAt` fallback + quota-throw resilience); predicted-setlist (pre/during/post-show modes + drag-reorder + matched-out-of-rank dim opacity); share-card unit + share-card-button + share-card-preview (popup_blocked, toBlob timeout, hit-highlight rendering); setlist-section (case-1 case-2 case-4 + first-visitor entry + corrupt-localStorage fallthrough + D-7 gating); eventTiming (helper boundary cases + strict-future guard regression); confirmStatus (60s boundary inclusivity + ISO-string acceptance + malformed/unknown fallbacks); useLocalConfirm (hydration scoped per event + idempotent toggle + flag-gated POST + network-error swallow + StrictMode-safe side effects).
- **Operations footnote — Haiku review hook.** The recurring 2026-05-06 timeout pattern hit five separate commits across PRs #281 / #282 / #283 (`[skip-review]` invoked on three; Sonnet prepush absorbed the review load on every push and caught nothing the local checks missed, CodeRabbit caught everything substantive at PR-time). Diffs ran 50–270 LOC each — well under typical Haiku budgets — so the issue is API-side latency, not commit-size. The two-tier review (Haiku commit + Sonnet push + CodeRabbit PR) held up cleanly even with the middle tier degraded. Worth revisiting if the pattern continues into Kobe-week reviews.

### v0.9.11 (2026-05-05)
- **Phase 1B Week 1 — `<SongSearch>` v1 shared song-picker component.** Three Phase 1B/1C surfaces all need a song search input + result list + selection callback (wishlist on the event page, prediction tab inside SETLIST, the Phase 1C `AddItemBottomSheet`); building one shared component now lets v2 (Week 3) bolt on the variant 2-stage picker without re-platforming the call sites (#272). Lands ahead of the 2026-05-16 user-open for the 5/23 Kobe wishlist + prediction window. New `src/components/SongSearch.tsx` ("use client") + `GET /api/songs/search` route. Search backend keeps parity with the existing `/api/admin/songs` pattern — Prisma `contains: { mode: "insensitive" }` over `originalTitle` + `SongTranslation.title`; the task spec's "pg_tsvector via `src/lib/search.ts`" framing was aspirational (`src/lib/search.ts` is in-memory client-side filtering only, no tsvector indexes exist) and gets revisited in Phase 2 alongside the Meilisearch swap-in. **Result scope** is base-version-only by default (`baseVersionId IS NULL`) — variant matching for wishlist + prediction is handled at *scoring* time via `baseVersionId`, not at *input* time, so fan pickers never need to surface variants. Admin opts in via `?includeVariants=true` to retain its current ability to record a setlist row against a specific variant ("Dream Believers (SAKURA Ver.)") — a Week 1 escape hatch that keeps `SetlistBuilder` from regressing while the variant 2-stage picker is held for v2. **Result-list hygiene** uses `AbortController` to cancel the prior in-flight fetch on every keystroke (not only when the next debounced fetch fires 300 ms later), on `handleSelect`, and on unmount — without this, a fast-resolving prior fetch can land during the new query's debounce window and clobber `loading=true` with stale `setResults` + `setLoading(false)`, briefly rendering stale rows. **`AbortError` is treated as a silent no-op** so the cancelled fetch's catch path doesn't fight the newer fetch over loading/results state. **Locale-decoupled rendering**: results carry full `originalTitle` + `translations[]` so the client renders via the canonical `displayOriginalTitle` / `displayNameWithFallback` helpers (no server-side commitment to a single locale). **i18n-context-agnostic**: takes a required `texts: { placeholder, loading, noResults }` prop instead of calling `useTranslations` — admin layout (`src/app/admin/layout.tsx`) intentionally omits `NextIntlClientProvider` per CLAUDE.md's admin-Korean-only carve-out, so an `useTranslations` call inside the component would have crashed at runtime the first time `SetlistBuilder` rendered it; admin passes Korean literals directly, fan callers in Week 2 will compose `texts` from their own `useTranslations` scope. **Basic combobox a11y**: input has `role="combobox"` + `aria-expanded` + `aria-haspopup="listbox"` + `aria-controls={listboxId}` (via `useId()`) + `aria-autocomplete="list"`; dropdown gets `role="listbox"` with the matching id; rows get `role="option"`. Full keyboard nav (↑↓ Esc + `aria-activedescendant`) deferred to a follow-up. **Admin SetlistBuilder migrates to `<SongSearch>`**; the now-dead `useEffect` pre-fetch of `/api/admin/songs` and its unused `songs` state (a leftover that ESLint had been flagging as unused since the original SetlistBuilder shape) get deleted in the same commit, eliminating an unconditional full-song-list fetch on every admin event open. The "+ 새 곡 추가" link stays alongside as an admin-only sibling element. The `as unknown as SongOption` cast that bridged `SongSearchResult` → `SongOption` got swapped for an explicit field-by-field projection so future shape drift on either type fails at the construction line instead of silently propagating undefined. Route hardening: `findMany` wrapped in `try/catch` returning `NextResponse.json([], { status: 500 })` on Prisma error so the client's `await res.json()` parse path stays predictable; `where` typed `Prisma.SongWhereInput` (was `Record<string, unknown>`, which silently stripped Prisma's compile-time validation of field names and filter shapes); `aria-label` mirrors `placeholder` so a custom-placeholder caller doesn't desync the visible label and the accessible name. 12 route tests + 9 component tests, including a stale-fetch race regression that resolves an old `"a"` query AFTER a newer `"ab"` query has already written its results — without the `AbortController` fix, the assertion would fail because the late `"a"` resolution would clobber state. Push-time review hook caught nine separate findings across the iteration (race, ARIA-less input/listbox, type bypass, missing error path, unsafe cast, runtime i18n crash, …) — all addressed in fix-up commits before the push went through; one declined with rationale (`SongSearchResult.id: number` vs the hook's defensive suggestion of `string` — `serializeBigInt` at `src/lib/utils.ts:30` uses `Number(value)` and the rest of the admin chain assumes number). v2 (Week 3) adds the variant 2-stage picker for the 1C bottom sheet without disturbing the v1 callers — `onSelect(song)` shape is forward-compatible for `onSelect(song, variant?)`.
- **F18 — Suppress UnitBadge for solo-Artist credit on non-solo rows.** Event 43's "Love it!" / "Wonderful Trip!" rows (ad-hoc unit-stage entries) rendered a single performer's name as a `<UnitBadge>` tinted by `resolveUnitColor`'s mystery-slug-hashed palette fallback, instead of the canonical `<FallbackUnitBadge>` whose label is derived from the row's `stageType` via `t(\`stageType.${item.stageType}\`)` — so "유닛" / "스페셜" / "솔로" depending on the actual stageType the operator entered (event 43's rows are `stageType="special"` and render "스페셜") (#271). Cause: `SetlistRow.tsx` honored any first `SetlistItemArtist` credit as a `unitArtist` whenever `stageType !== "full_group"`, so a row entered with one performing member's solo `Artist` row got promoted to a unit-badge render. Surgical predicate fix narrowly suppresses the demonstrated misfire — a solo-type Artist credit on a non-solo stage type now falls through to the existing `<FallbackUnitBadge>` branch (matches PR #190 D4b's "never expose half-formed unit data publicly" rule). All other type combinations (unit-type / group-type Artist on unit/special; solo on solo) render unchanged — verified by 4 new tests in `setlist-row.test.tsx` covering F18 + a special-stage-type-with-group-Artist no-regression + a solo no-regression + a desktop col-3 multi-performer comma-join lock-in (the desktop column-3 performer list `item.performers.join(", ")` was independently correct and stays so). AU-1 (final `unitName` disposition) and AU-2 (admin `SetlistBuilder` validation) remain deferred per task spec — this PR is the public-render fix, not the data-shape fix.
- **F17 — Trending TOP3 widget shows all four reaction-type counts.** Day-1 (5/2 Fukuoka) surfaced that the trending widget *ranked* correctly (sums all four reaction-type counts per `SetlistItem`) but *displayed* only the single highest-emotion count under each ranked item, so once per-type counts grew past single digits the displayed number under-represented total engagement (#270). Replaces `topReaction` (single max-emotion projection) with `reactionCounts` (raw per-type map) on `TrendingSong`; renders all four counts in canonical `REACTION_TYPES` order, mirroring `<ReactionButtons>`'s per-row strip. Same shape change applied to both the SSR `getTrendingSongs` path and the client-side `deriveTrendingSongs()` (the live polling path) so the polling tick can't desync the display. Two follow-up refinements landed on the same branch before merge: (a) per-type counts get explicit `aria-label` so screen readers hear "기다렸어 3, 최고 5, 깜짝 2, 감동 1" instead of bare numbers (the emoji glyphs themselves are decorative), and (b) the original `= N total` suffix gets dropped and zero-count types are omitted from the strip, on the reasoning that the four-emoji row already names the ranking criterion implicitly and trailing zero entries on a low-engagement song dilute the visual rank ordering. Empty-state nudge unchanged. 18/18 trending tests pass (was 16, +2 new regression guards including a max-single-vs-aggregate ranking inversion case).
- **CI: event-detail TTFB perf check now measures on-runner local builds, not Vercel previews.** v0.9.10's `event-detail-perf.yml` (#264) polled the Vercel preview URL for the PR's head SHA and compared warm TTFB against the latest successful base-branch preview. Two systematic problems surfaced on PR #265 (a doc-only release-notes PR): (a) PR #265 was opened off a stale dev — its preview deployed un-optimized pre-#260 code (~4.82 s) while the base preview had the optimized relationJoins build (~1.35 s), so the workflow flagged a fake `+257.9%` regression that was actually git history rather than the diff; and (b) per-deployment Vercel preview URLs scale to zero — hitting a low-traffic preview after idle = 3–4 s cold-start even with the warm-up loop, and the load balancer can route each request to a fresh instance, so comparing two preview URLs in different "warmth" states is noisy by design (#267). Two changes in this PR: `paths-ignore: ['**/*.md']` skips the workflow entirely on doc-only PRs (markdown literally cannot affect TTFB; saves CI minutes too), and the measurement target switches from Vercel preview URLs to **on-runner local prod builds** of `refs/pull/N/merge` (GitHub's auto-generated test-merge of PR HEAD onto current base — what `dev` would actually look like post-merge) and the base branch tip. Both built on the same runner with the same Node + same `DEV_DATABASE_URL_UNPOOLED`, measured under identical conditions immediately after warm-up. Stale-PR false positives become impossible because the merge commit is by construction (current dev + the diff); cold-start asymmetry disappears because both servers boot on the same runner. Trade-off: lose the ability to detect Vercel-edge-specific regressions (function cold-start, regional drift) — acceptable, because the F20 regression class (Prisma include shape / DB-query fan-out) is what we actually want to catch and that's now measured more reliably. Workflow timeout bumped 25 → 30 min for two builds + two measurements; `pkill -f "next start"` + `fuser -k 3000/tcp` paranoid cleanup between builds; 30 s polling readiness loop on `localhost:3000`. Status check `perf/event-detail-ttfb` unchanged and stays non-blocking (not in branch-protection required-checks).

### v0.9.10 (2026-05-04)
- **F19 — slug-generator unification + KO transliteration.** Per-entity slug generators (Artist / Song / Event / EventSeries / StageIdentity) were separately implemented; the Day-1 launch hit empty-slug-on-JP-only-title (`ペレニアル` → `""` → unique-constraint collision against existing empty-slug rows), got hotpatched on the Song path only via PR #247. The unification refactor was held out of the launch window for safety and lands now (#248) ahead of the 5/23 Kobe Phase-1C user-registration window — after Phase 1C ships, every slug path will be hit by user-supplied JP/KO/EN titles, so the per-route inconsistency would have become user-visible. Three shared helpers in `src/lib/slug.ts` — `validateCanonicalSlug` (sync round-trip check, returns `null` on non-canonical so callers map to a 400 with no silent rewriting), `deriveSlug` (async pipeline: ASCII strip → `es-hangul.romanize` if Hangul detected → kuroshiro romaji → `""`), `resolveCanonicalSlug` (combined entry point, three-way classification of `rawSlug`: absent → auto-path, non-string → 400 invalid input, non-canonical string → 400, canonical string → pass-through). Replaces per-route `resolveAdminSlug` across every admin POST route plus the slug-generator preview tool. `next.config.ts` extends kuromoji dict tracing from `songs/**` only to `slug-generator`, `artists`, `events`, `event-series`, and `artists/[id]/stage-identities` — the slug-generator preview was silently producing `""` on Vercel for JP names because its lambda bundle had no kuromoji dict files, so an operator typing `ペレニアル` into the preview saw `(변환 실패)` even though saving the song would have worked. **KO transliteration via `es-hangul`** was added on the merge branch (the original task assumed es-hangul was already wired in `slug.ts` but only `josa()` was used elsewhere): `transliterateKoreanToRomanized` is sync (es-hangul is pure-JS, no dict files) so the KO path needs no `next.config.ts` change, and the `/[㄰-㆏가-힣]/` Hangul detection lets pure-KO names skip the kuroshiro init cost. Sample: `테스트아티스트` → `teseuteuatiseuteu`, `페렌` → `peren` — pre-PR these all fell to `${model}-${ts}`. P2002 catch on each route now uses `isSlugUniqueViolation(target)` to distinguish slug from translation-locale-composite or join-table uniques (admin nested creates raise P2002 for many constraints, not just `slug`); songs's existing P2002 retry loop preserved + bails on non-slug P2002 so it doesn't burn its 3 attempts re-rolling slugs against an unrelated constraint. Stage-identity form's sequential POST loop tracks completed indices so a mid-list failure prunes saved items from the retry list — fixes the duplicate-create bug where a randomUUID-suffixed stage-identity got re-saved on every retry. Three admin forms (Artist / Event / EventSeries) gain the same `try/catch/finally` + `body.error`-surfacing pattern SongForm got in #247. Day-1 Song-path hotpatch absorbed into the unified helper.
- **F15-RC — OG image cold-start hardening.** The Day-1 launch tweet (and Day-2's separate-URL tweet) failed to render their OG cards on Twitter; T-2h smoke tests via Twitterbot UA passed both days but reproduced on the actual tweets anyway (#259). Two days of separate-URL reproductions ruled out the Twitter-side-transient hypothesis (A2′) and confirmed cold-start lambda crash (A2): `loadOgFonts()` was throwing on Vercel's lambda cold start when one of the eight Noto Sans subset WOFFs hit transient I/O failure, taking the whole `/api/og/*` route 5xx with it — Twitter then negative-caches the scrape failure for ~7 days, so a single cold-start I/O blip translates to a week of broken share previews on the affected URL. Fix replaces the bare-`readFile` chain with per-file `AbortController` + 5s timeout: ENOENT / EACCES / AbortError all resolve to `null` instead of throwing, partial loads emit `Sentry.captureMessage("og.fonts.partial_load", { extra: { loaded, expected, missing } })` for production observability, and the loaded subset is cached. With zero fonts loaded `@vercel/og` falls back to the `Geist-Regular.ttf` Next bundles inside its compiled `@vercel/og` copy — bare OPENSETLIST card still produces a valid 200/PNG, blocking Twitter's negative-cache trap at the source. Path A (per-`readFile` timeout + partial tolerance) chosen over Path B (module-init hoist) because Path B doesn't actually fix the failure mode (if init fails, the lambda still 5xxes; Vercel doesn't auto-retry init). CR-driven follow-ups: kept plain `setTimeout` (not `AbortSignal.timeout()`) for Vitest fake-timer compatibility; switched from `Promise.race` to `AbortController` so the I/O is actually cancelled instead of letting Node keep buffering after timeout; clarified inline that the fallback font is `Geist-Regular.ttf` from Next's compiled `@vercel/og` copy (not the standalone npm package's Noto Sans). Live A2 confirmation will come from `og.fonts.partial_load` Sentry messages firing on cold starts post-deploy. F15-CI (preview-deploy smoke job + nft-trace assertion) is now unblocked as a follow-up.
- **F20 — Event Detail page TTFB cut from ~4.0s to ~0.83s warm (−79%).** Day-2 launch surfaced operator-quote-worthy slowness on the event detail page ("'잘못됐나' 의문이 들 정도"). Combined fix across five PRs: **#260** wraps `getEvent` in `react.cache()` so the duplicate call across `generateMetadata` + page-component collapses to one DB roundtrip per request, and filters every nested `translations` block in the include to `{ locale: { in: [locale, "ja"] } }` — most rows previously fetched were translation rows that got filtered to a single locale at render time. Display helpers cascade through the parent's `originalName`/`originalShortName` columns when no translation matches, so the filtered shape never renders blank; the `"ja"` half backs `displayOriginalName`'s sub-line cascade for the canonical-original safety net. `~4.0s → ~2.7s` (−33%). **#261** parallelizes `getEvent` with the i18n + per-event helper batch (both only need `eventId` from the URL, no dependency forcing serial execution) and collapses `getTrendingSongs.q2`'s 4-statement Prisma fan-out into one statement via `$queryRaw` with a LATERAL JOIN. Trending stays serial-and-conditional because the skip-when-ongoing semantics need `event.status` from `getEvent` first; always-launching trending in the batch would reintroduce ~940ms of pure DB waste during live shows. `~2.7s → ~2.28s` (−17% additional). **#262** enables Prisma's `relationJoins` preview feature (`previewFeatures = ["relationJoins"]` in `prisma/schema.prisma`'s generator config — Prisma 7.7, preview since 5.7 ~Dec 2024). Switches the default relation-load strategy from per-relation SELECTs (DataLoader-style fan-out) to a single LATERAL JOIN with JSONB aggregation. Per-request SQL statement count drops 35 → 9; the 9 remaining are unavoidable `Promise.all` branches (`getEvent`, `reactionCounts`, `impressions` main+count, trending q1/q2/q3, helper joins). `~2.28s → ~0.83s` (−64% additional, −79% cumulative). Local Postgres planner verified — no query-plan regressions on the LATERAL JOIN + `JSONB_AGG` shape. **#263** drops the `$queryRaw` block from #261 — `relationJoins` makes the same flattening automatic, so the original `prisma.setlistItem.findMany` shape is now equivalent on the wire (~70 fewer lines, Prisma-generated types instead of hand-rolled column-name types). **#264** adds a non-blocking GitHub Actions job (`.github/workflows/event-detail-perf.yml`) that polls the Vercel preview for the PR's head SHA, measures warm TTFB across 6 (locale × event) combos × 2 passes vs the latest successful base-branch deployment, and posts a delta as a PR comment + commit status `perf/event-detail-ttfb`. Deliberately non-blocking (status check not in branch-protection's required list): wall-clock TTFB is too flaky to gate merges on, but the comment surfaces drift so reviewers can decide intentional vs regression. F16 (N+1 query on setlist endpoint) and F21 (connection-acquisition timeouts at audience ramp) likely auto-resolve as side effects of the include-shape rework — verify post-tag via 24h Sentry watch.

### v0.9.9 (2026-05-02)
- **F14 Day-2 mitigation — Supabase pooler EMAXCONN at peak polling × audience.** Day-1 launch (2026-05-02 KST) hit `DriverAdapterError: max client connections reached, limit: 200` twice within the first 11 minutes (T+5, T+11) at the audience-arrival ramp (#253). 5-second polling × concurrent viewers × per-request connection acquisition saturated the Supabase free-tier 200-client PgBouncer ceiling. Two complementary code-only levers: **(A) asymmetric polling cadence** — `useImpressionPolling` default cadence 5 s → 30 s; setlist + reactions intentionally stay at 5 s (reactions ride on `/api/setlist`'s response, so setlist cadence governs reaction freshness — the live-show experience must not regress). Impressions are conversational; ≤ 33 s cross-user freshness is fine, and the submitter's own UX is unaffected because `EventImpressions.handleSubmit` merges the POST response synchronously. Translation is on-demand via 🌐 click, not polled. Per-viewer poll rate 0.40 → 0.233 polls/sec. **(B) `PrismaPg max: 3`** — without `max` set, pg defaults to 10 connections per pool, which combined with the 200-client ceiling means EMAXCONN at just 20 concurrent warm Vercel lambdas (matches the Day-1 ramp). Pinning `max: 3` aligns the per-lambda budget to the actual peak concurrent DB-touching query count on user-facing pages (event detail's render-time `Promise.all` peaks at 3: 1 `getReactionCounts` + 2 internal to `getEventImpressions`'s findMany/count fan-out). Other user pages peak at ≤ 2; admin dashboard fans to 8 but is operator-only. Zero latency penalty on user pages; raises ceiling to ~66 concurrent warm lambdas. Combined headroom A + B: ~113 concurrent viewers before saturation (~5–6× Day 1). Also set `idleTimeoutMillis: 20_000` and `connectionTimeoutMillis: 10_000`. Verifications already done (no code change): `DATABASE_URL` confirmed on transaction pooler (port 6543), Prisma singleton in `src/lib/prisma.ts` matches the canonical Vercel pattern. Out-of-scope escalation levers if Day-2 still saturates: drop `max: 1` (serializes parallel queries, +30 ms penalty, ceiling 200 lambdas), push impression cadence to 60 s, Supabase Pro upgrade. Phase 1C Realtime cutover (5/23 Kobe target unchanged) is the structural fix this patches around.
- **F13 Day-2 mitigation — translator returns `[]` on short Korean inputs without glossary matches.** Reproducible in `/admin/translation-debug` for inputs like `시작한다`: OpenAI raw response was the literal `[]` (empty array), `parseMultilingualResponse` correctly rejected "no object entry", route returned 502, fan saw the `translateFailed` toast (#254). Root cause: the Hasunosora system prompt is structurally a glossary — Section [1] (고유명사 사전) consumes ~95 % of the 1073-token prompt; only the opener and format-rule line carry the translation framing. `gpt-4o-mini` reads the prompt as "extract glossary entries from input" on inputs with zero glossary matches → finds none → returns `[]`. Length correlation is statistical (short inputs are likelier to lack glossary entries), not a real threshold. Editing the system prompt would break the > 1024-token implicit-cache invariant ([[v0.8.11 release notes]]), so the fix lives on the **user turn**: explicit `task: translate ... into ko, ja, and en` framing plus an "always return one JSON object inside an array" clause that forecloses the empty-array failure mode. Also adds a redacted Sentry breadcrumb at the route's translator-failure catch (provider / locale / error name / text length / `targetLocale` — never content) so future regressions surface with metadata for triage. Defensive optional-chain on `impression?.content?.length` future-proofs the breadcrumb against catch-widening. The originally-recommended min-length guard would have masked this without identifying the framing flip.
- **F13 follow-up — pair the Sentry breadcrumb with `Sentry.captureMessage`.** CR caught that a breadcrumb alone is dead weight — it only attaches to events captured later in the same scope, and the translator-failure catch returns 502 immediately, so the failure was being recorded in `console.error` but never reaching Sentry, defeating the visibility goal that motivated the breadcrumb in the first place (#257). Pair the existing breadcrumb with `Sentry.captureMessage("translator.translate_failed", { level: "error", tags, extra })`. Deliberately **not** `captureException(err)` — that would re-leak `err.message` / `err.cause` / `err.stack`, which provider SDK errors are known to populate with the user's impression text. Same metadata-only redaction discipline as the breadcrumb (`provider` / `sourceLocale` / `targetLocale` / `errorName` as searchable tags; `textLength` as extra) — no content fields anywhere. Shipped to main via PR #257 ahead of the v0.9.9 tag; back-ported into the v0.9.9 release annotation so the two stay aligned.

### v0.9.8 (2026-05-02)
- **Admin song create — production hotfix for Japanese-titled songs.** `POST /api/admin/songs` was 500ing with `PrismaClientKnownRequestError P2002 (slug)` when the operator tried to add a Japanese-titled song (#247). Two root causes that compounded: `generateSlug("ハナムスビ")` strips all non-ASCII to `""`, so the first Japanese song to claim the empty slug won the `Song.slug @unique` constraint and every subsequent one collided; song variants legitimately share `originalTitle` (`"Dream Believers"` + `"Dream Believers (SAKURA Ver.)"` both produce `dream-believers`) and would have collided on the ASCII path too. Migrate the route to `generateUniqueSlug` (kuroshiro transliteration → `"hanamusubi"`, `-2`/`-3` suffix on existence check, `song-${Date.now()}` fallback if both inputs are empty) — the same pattern artists / events / event-series already used. Admin-supplied slug is accepted verbatim, validated via a `generateSlug` round-trip (input must already be canonical lowercase-alphanumeric-with-hyphens), 400 with a Korean format hint on non-canonical input — no silent normalization. P2002 from concurrent auto-gen requests retries with a fresh slug up to 3 times before returning 409 (the existence-check-then-insert window is racy; a loser used to get 409 instead of `hanamusubi-2`). Admin-supplied slug collision still returns 409 immediately since the operator's intent was specific. `SongForm` rewritten with `try/catch/finally` so `setLoading(false)` always fires (incl. fetch reject — the prior code locked the submit button until refresh on a network blip), and the alert now reads `body.error` from the response so the operator sees the actual reason ("슬러그 'hanamusubi'가 이미 사용 중입니다", or "URL-safe 형식이어야 합니다") instead of bouncing every failure through a generic `저장에 실패했습니다.`.
- **Vercel kuromoji dict tracing.** First Japanese-titled song attempt on Vercel dev produced slug `song-1777697087979` instead of the expected `pereniaru` (#250). Kuroshiro init was throwing at runtime because `node_modules/kuromoji/dict/*.dat.gz` (the IPADIC binaries) weren't being traced into the deployed function bundle — `serverExternalPackages: ["kuromoji"]` in `next.config.ts` keeps the JS module out of the Next bundle but doesn't carry the data files alongside, and the analyzer loads them via a runtime path the file tracer can't see. Same shape of issue the `@vercel/og` font tracing fix in v0.7.1 documented. Add `./node_modules/kuromoji/dict/*.dat.gz` to `outputFileTracingIncludes` for `/api/admin/songs/**` (~17 MB compressed, scoped to the only route that uses `generateUniqueSlug` today). Replace the bare `catch {}` in `transliterateToRomaji` with `Sentry.captureException` (lazy-imported so the module stays safe in tests/scripts) — the silent catch is exactly what hid this bug until a real song hit it; future regressions will surface in Sentry tagged `source: transliterateToRomaji` with the failing input.
- **Admin sidebar — link to 슬러그 생성기.** `/admin/slug-generator` already existed but had no nav entry; operators had to type the URL by hand (#249). Drop a link next to the other utility entries (CSV 가져오기, 번역 디버그). Verbatim label match to the page heading.

### v0.9.7 (2026-05-01)
- Replace `src/app/favicon.ico` — the white-triangle-in-black-circle Vercel scaffold default committed at project init (`1b6e3e3`) — with a brand-rendered ICO sourced from `src/app/icon.svg` (#244). New `scripts/generate-favicon.mjs` rasterizes the SVG once at 256 px via `sharp`, downsamples for the smaller layers, and writes the ICO container by hand with **PNG-encoded layers** at 16 / 32 / 48 / 256 — the format every modern browser (incl. iOS Safari since ~2007) decodes natively. The 256 layer is what iOS Chrome on a Retina iPhone picks for the address-bar indicator (effective render is 32–48 logical px = 64–96 device px); without it the browser falls back to the "gray globe" no-favicon placeholder. Why a manual encoder: an earlier iteration with `to-ico` produced an all-BMP file that iOS WebKit silently rejected — the scaffold default worked specifically because it shipped a 256 PNG layer alongside its BMP layers. `sharp` added as a devDep (~30 MB on disk, runtime bundle untouched). Run via `npm run generate-favicon` after editing `icon.svg`. Caveat: Vercel preview deployments are SSO-gated, so `/favicon.ico` returns auth-challenge HTML to iOS Chrome's tab fetch (which doesn't carry the cookie) — favicon work can only be reliably tested on dev preview with protection disabled or post-merge on prod.
- Impression report confirm modal + 🚨 / 🌐 emojis on the report and translate actions (#243). Tapping 🚨 used to delete-report immediately with no confirmation; a slip would auto-hide a comment after three reports without recourse. New modal explains the report consequence and offers Cancel/Report. Full a11y polish in CR-driven follow-ups: dialog ARIA refs (`role="dialog"`, `aria-modal`, labelled-by/described-by), Escape-to-close, Tab-focus trap inside the modal, initial focus on Cancel (the safer default), and focus restored to the trigger button on close. Mobile and keyboard-only flows behave the same.

### v0.9.6 (2026-05-01)
- Live event page sidebar now ticks from the same poll cycle as the setlist itself (#237). The three sidebar cards (`EventHeader` counts, `UnitsCard`, `PerformersCard`) used to render once at SSR and stay frozen for the rest of the session — adding a song mid-show didn't bump the songs-count pill, didn't surface the new performer's pill, didn't show the new unit row. Lift `useSetlistPolling` from `<LiveSetlist>` into a new `<LiveEventLayout>` wrapper that sits above both columns; re-derive the four sidebar values from each poll tick. The ~230 lines of guest-set + unit/performer walks moved into pure helpers in `src/lib/sidebarDerivations.ts` so the same code runs server-side at SSR and client-side on every poll. Setlist UI types lifted to `src/lib/types/setlist.ts` to keep `lib/` from importing across into `components/`. CR-flagged hydration-mismatch risk fixed by gating the client recompute on `lastUpdated !== null` — first paint reuses the SSR-pre-formatted compact-number string until the first poll lands. `/api/setlist`'s include shape now carries `stageIdentity.artistLinks` so a poll-introduced performer can render under their unit (mid-event guest changes remain unsupported).
- Event OG card: drop city + venue from the subtitle (the event name typically already encodes the venue, e.g. `Day 2 · Marine Messe Fukuoka` — no room to say it twice on a 1200×630), and add an optional `?s=<status>` pin via `&s=${getEventStatus(event)}` in the page metadata's og:image URL (#238). When the OG route receives a valid pin, it overrides the clock-derived status and serves with a long cache (1 h + 24 h SWR); when missing or unrecognized, behavior is byte-for-byte unchanged. Why: social platforms cache OG unfurls aggressively (X/Slack ≈ 7 d, Discord ≈ 1 d, Facebook indefinite) — independently of our `Cache-Control` — so a link shared at T-2 h would otherwise freeze its "upcoming" pill in chat long after the event went live. Pinning the status into the URL means freshly-shared links are self-describing forever; pre-existing shares (no `&s=`) keep clock-derived behavior. `RESOLVED_EVENT_STATUSES` tuple promoted to `src/lib/eventStatus.ts` so the `?s=` validator and the `ResolvedEventStatus` type derive from a single source.
- Trending Top-3 card on the event detail page: switch the title cascade from translation-only to original-primary (#235). Every other song listing on the surface (main setlist row, song detail header, member/series history) uses `displayOriginalTitle` to put the original-language title primary with the localized translation as a muted sub-line; the trending card was the lone outlier rendering only the localized title. `TrendingSong` payload now carries `mainTitle` / `subTitle` / `variantLabel`; `<TrendingSongs>` renders the same shape as `<SetlistRow>`'s `SongTitleBlock` — original primary, localized title in muted text alongside, variant in parens — on one truncating line. Both the SSR helper (`getTrendingSongs`) and the client derivation (`deriveTrendingSongs`) updated.
- Impressions: cursor-paginated "see older" with a polling hot-path optimization (#239). The list previously fetched the latest 50 in one shot with no way to page further; older comments were unreachable. New `before=<cursor>` query param + a "see older" CTA on the impressions card; cursor-encoding helper extracted to `src/lib/impressionCursor.ts`. Page-size constant lifted out so SSR seed + client load-older + tests share one number. `/api/impressions` now returns `{ items, total? }` with `total` only computed when `?includeTotal=1` — the polling tick (every 5 s during ongoing events) skips the `count()` round-trip entirely; `useImpressionPolling` only requests the total on the initial fetch. Cuts the per-tick query time on busy events and keeps the count fresh enough for the visible "총 N" badge.
- Soft-deleted-item exclusion in home/events-list song count (#234). The badge that previously read "12 songs" on the home upcoming card and the `/events` list was double-counting: it didn't filter `isDeleted=false` on the setlist-items rollup, so retracted rows kept inflating the number, and it counted non-song row types (MC / video / interval). Fix scopes the count to `type === "song"` items with `songs.some({})`; constraints centralized as a `SONG_COUNT_WHERE` Prisma filter in `src/lib/setlistCounts.ts` and reused by both surfaces.
- Admin SetlistBuilder: new SetlistItem performers default to the event's full performer roster (#236) so the operator doesn't have to re-tick every member on every row. The previous empty-default required ~7 clicks per setlist item on a 9-member roster.

### v0.9.5 (2026-04-30)
- Home upcoming card now refines D-0 into a live "X hours left" / "X mins left" badge once the next event is < 24 h away. Server bucketing runs on UTC day boundaries, so D-0 covered anything from a few minutes to ~23 h 59 m — too coarse at this range. `UpcomingCard` overrides D-N client-side and ticks every 30 s; initial state stays `null` so SSR + first hydration still emit the server-rendered label (no flash, no hydration mismatch). Plural-aware copy added for ko / ja / en (`#시간 남음` / `あと#時間` / `# hours left`, with min variants).
- Sentry: filter Next.js control-flow throws in `onRequestError`. Sentry's `captureRequestError` was forwarding every digest from `notFound()` and `redirect()` as an unhandled exception, so each bot/crawler probe (`apple-touch-icon`, `.well-known/*`, random `.php` scrapers) opened a Sentry alert. Inline digest check for `NEXT_NOT_FOUND`, `NEXT_HTTP_ERROR_FALLBACK;404`, and `NEXT_REDIRECT;*` short-circuits before SDK capture; genuine exceptions still flow through. 404-rate monitoring belongs in Vercel analytics, not the exception tracker.

### v0.9.4 (2026-04-30)
- Admin sidebar stacks above main on mobile so data-entry forms get the full viewport width. The `flex` body + `w-52` (208 px) fixed-width sidebar consumed 55–65 % of a 320–375 px screen, leaving the form column ~110–170 px — too narrow for artist edit, song edit, the CSV import textarea, or the event setlist builder. Pure className swap on `src/app/admin/layout.tsx`: `flex-col lg:flex-row` body, `w-full` sidebar that collapses to an `overflow-x-auto` horizontal nav strip on mobile, `border-b` instead of `border-r`. Desktop layout (lg+ ≥ 1024 px) is byte-for-byte unchanged. Operator-only surface per CLAUDE.md's admin-scope exemption — no drawer, no client component, no redesign.
- Misc: `apple-touch-icon.png` + `apple-touch-icon-precomposed.png` (180×180, rendered from `src/app/icon.svg`) added to `public/` to silence Sentry 404 alerts triggered by iOS Safari / crawler probes. Shipped to main via hotfix #226 ahead of the v0.9.4 tag; back-ported to dev via #228 so the two branches stay aligned.

### v0.9.3 (2026-04-30)
- Member-page data-correctness fix: `getMember`'s `performances` and `eventPerformers` includes had no soft-delete `where` filter, so junction rows (`SetlistItemMember`, `SetlistItemSong`, `EventPerformer`) attached to soft-deleted parents still leaked into the songs / history aggregations. Symptom in prod: a variant song that had never been performed live surfaced on a member's 자주 부른 곡 list with `timesPerformed: 1` because the only `SetlistItemSong` row referencing it sat under a retracted (soft-deleted) `SetlistItem`. Three-tier filter added on `performances` (`setlistItem.isDeleted=false` + `event.isDeleted=false` + `songs.song.isDeleted=false`) plus matching `event.isDeleted=false` on `eventPerformers`. Other detail pages (artist / song / series) already filtered correctly — leak was isolated to `getMember`.

### v0.9.2 (2026-04-30)
- Mobile horizontal-overflow fix on the four detail pages (artist / member / song / series). Their sidebar+main wrapper was `grid lg:grid-cols-[280px_1fr]`, which on mobile fell back to implicit `grid-auto-columns: auto` and grew the track to fit the widest nowrap descendant's `min-content`. Long song titles, series subtitles, or event names therefore stretched the cards wider than the viewport while `<main>`'s `bgPage` only painted to the viewport edge — visible as a colored gutter beside every card and horizontal page scroll. Adding `grid-cols-1` clamps the mobile track to `minmax(0, 1fr)` so cards fit the viewport and the existing ellipsis truncation engages as designed. Event detail page already used `lg:grid` (block on mobile) and was unaffected.

### v0.9.1 (2026-04-29)
- Avatar-initial overhaul: member-page hero character avatar + artist list rows + member chips on the artist detail page now source the round/square glyph from the curated short name (canonical-script for the member hero, locale-primary for artist surfaces) instead of the full name's first character. VA avatar deliberately retained on the full-name first character (operator preference). New `resolveOriginalShortLabel` helper extracted to `src/lib/display.ts` with focused tests pinning the fallback order.
- Vibrant fallback palette for color-pending unit-type artists — Tailwind 400-500 family replaces the muted Material 700 set; preserves length + hash + per-slug determinism, no semantic-color collisions. New `Artist.color` column wired into the artists CSV admin import (preserve-on-missing semantics — column omitted from header keeps existing color, empty cell clears it).
- Reaction column re-tuned: 4 buttons no longer wrap on Windows / Segoe UI Emoji (260 → 280px reactions column on the desktop setlist grid). Polling race fix so a poll mid-mutation no longer overwrites the optimistic count — `pendingPollCounts` stash drains when `loading` returns to null.
- UI polish across home, history, songs, series, member, and song surfaces: home always renders upcoming + recent boxes (no full-empty state); history badge centered + trailing column shrunk + status column widened for badge breathing room; song-count cells consistent on member + series (single-line, digit-aligned); series-songs rows clickable to song detail; song-page `?tab=variations` clamps to history when no variations; desktop event-row date format includes year.
- i18n: 한줄평 → 한줄감상 (less old-fashioned register; postpositions preserved by ㅇ받침-final endings). Home upcoming wording fixed (앞으로, not 최근). En "show" → "event" for clarity.
- Schema + code hygiene: `SetlistItem.note` hidden from the public event view (Japanese-only data, both SSR and `/api/setlist` polling); `displayName*` helpers unified with full-by-default; admin import wraps `ArtistGroup` replace in `prisma.$transaction` so a partial-failure batch can no longer orphan multiple artists from their groups.

### v0.9.0 (2026-04-29)
- Top-to-bottom UI redesign across home, events list, event detail, artist list/detail, song detail, member detail, series detail, and legal pages — all rebuilt on a new shared-component system (`<InfoCard>`, `<TabBar>`, `<SectionLabel>`, `<StatusBadge>`, `<Breadcrumb>`, `<PerformanceGroup>`, `<LegCard>`, `<ColorStripe>`, etc.). 53 PRs since v0.8.17.
- Phase 1A ad-hoc unit + guest handling: operator-typed `SetlistItem.unitName` hidden from public surfaces (no per-locale translations); generic `<FallbackUnitBadge>` "유닛" / "ユニット" / "Unit" for unit-stage rows without backed Artist credit; guest performers + guest units carry "· 게스트" / "· ゲスト" / "· Guest" suffix; Pass-2 unit→members building skips guests so stale `artistLinks` from returning graduates don't pollute host-unit member sublists.
- Cross-page consistency: `LIVE` badge unified across all surfaces, shared `HISTORY_ROW_DATE_FORMAT` between event list and every detail-page history tab, `<SectionLabel>` adopted on previously-inline section headers, stats grid sub-labels uppercased + letter-spaced for English.
- Translation-primary identity names: `displayOriginalName` flipped — viewer's locale name is the headline (`main`), original-language name reads as the secondary `sub` line. Song / album titles kept original-primary (artwork identity).
- Schema: `Group.slug` (`@unique`, CSV upsert key), `Artist.color`, `Artist.category`, `Artist.isMainUnit`. `GroupCategory` enum reshape: `anime` + `game` merged into `animegame`, added `others` as catch-all. Two-stage prod migration (Prisma's AlterEnum + AddColumn ordering required a transient superset enum push first; details in the v0.9.0 deploy log).
- i18n: 세트리스트 spelling standardized across all surfaces, new `Common.home` noun for breadcrumb hierarchy crumbs (was the `backToHome` CTA), new `Event.guestLabel` key, `tourOngoingLabel` spacing fixed.

### v0.8.17 (2026-04-26)
- Reaction counts + trending TOP3 propagate via the existing 5s polling (F12 launch-blocker from rehearsal #2). `<ReactionButtons>` re-syncs when polling delivers a fresh map reference (prev-prop tracking idiom). Trending TOP3 rendered inside `<LiveSetlist>` and derived from the same `reactionCounts` driving per-item counts (single source of truth).
- `REACTION_TYPES` + `EMOJI_MAP` consolidated to `src/lib/reactions.ts` (previously duplicated across three files).
- SSR `getTrendingSongs()` (3 Prisma queries) skipped when `isOngoing` — `<LiveSetlist>` derives client-side on first paint anyway.

### v0.8.16 (2026-04-26)
- `SetlistItem.note` hidden from the public event view. Notes had Japanese-only data that read poorly for ko / en / zh visitors. Both SSR rendering and `/api/setlist` polling use Prisma `omit: { note: true }`. Admin SetlistBuilder unchanged — 메모 input still editable; saved data preserved.
- Per-locale `Meta.description` + `Home.description` rewritten from anime / game-specific to multilingual "open setlist database for live events (JP/EN/KR)" positioning, with natural translations in en / ko / ja.
- `<link rel="alternate" hreflang="…">` for ko / ja / en plus `x-default` → `/en` on the homepage. Languages derived from `routing.locales` (single source of truth); URLs built via `new URL()` for trailing-slash safety. Scoped to the homepage only — a layout-level canonical would tell Google to deprioritize translated content on every child page.

### v0.8.15 (2026-04-25)
- Soft-deleted `SetlistItem` rows no longer permanently hold their `(eventId, position)` slot — partial unique `WHERE isDeleted = false` in `post-deploy.sql`. Position auto-suggests `max + 1` for new items.
- `<EventStatusTicker>` auto-refreshes at status boundaries via a single `setTimeout(() => router.refresh())` at the next transition (`upcoming` → `ongoing` at `startTime`; `ongoing` → `completed` at `startTime + 12h`). Mounted on the event detail header and per-card in `<EventRow>`.
- Impression polling endpoints flipped to `private, no-store` + client hooks fetch with `cache: "no-store"`. Drops the SSR-seed remount clobber that was discarding fresh polled data on hydration.

### v0.8.14 (2026-04-25)
- JP OG cards no longer tofu out symbols like `～ ／ ★ ☆ ♡ ♥ ♪ ♫ ♬ ❀ ✿`. Eight numbered Noto Sans JP subset WOFFs registered as fallbacks in `OG_FONT_STACK` (~120 KB additional traced into the `/api/og/*` function bundle).
- `titleFontSize()` with CJK-weighted scoring shrinks long JP / KR titles (60/72 → as low as 30/32 px) before they hit the 2-line clamp, instead of mid-word clipping. Tier table + 31 boundary tests at scores 20 / 35 / 55 / 80 / 110 for both base=60 (event) and base=72 (artist / song). Wave-dash `〜` extended into the wide-char regex.

### v0.8.13 (2026-04-23)
- Admin dashboard `/admin` gains a 한줄평 card between 세트리스트 항목 and 감정 태그. Counts head non-deleted impressions; hidden rows (auto-hidden by `reportCount ≥ 3`) stay in the total so the dashboard reflects moderation backlog. Links to `/admin/impressions`.
- Vercel Speed Insights mounted in `[locale]/layout.tsx`. Real-user LCP / INP / CLS / TTFB alongside existing Sentry + GA4. SDK auto-detects `VERCEL_ENV` and no-ops locally. Admin area intentionally skipped to keep the RUM dataset focused on end-user experience.

### v0.8.12 (2026-04-23)
- Mobile fix: long series names now wrap instead of overflowing the viewport. `<EventGroup>` h3 and `<EventRow>` title / subtitle switched from `truncate` to `break-words`. Long Hasunosora series names ("러브라이브! 하스노소라 여학원 스쿨 아이돌 클럽 6th Live Dream Bloom Garden Party Stage") were spilling past the right edge at ~360 px on the home card variant + standalone `/ko/events` heading. Wrapping preserves full info across 2-3 lines.
- Language switcher uses "English" instead of the short code "EN", matching the native-name style of the other two options (한국어, 日本語).

### v0.8.11 (2026-04-23)
- Translation implicit-cache rewrite. The per-event glossary + placeholder substitution pipeline replaced with a 1074-token hardcoded Hasunosora system prompt that embeds the ko / ja / en glossary inline. One LLM call now returns all three locales — both non-source target rows cache per round-trip, cutting ja + en fanout from two calls to one. The stable prompt prefix sits over the 1024-token implicit-cache threshold on both Gemini 2.5+ and OpenAI.
- `Translator.translate(text, sourceLocale, signal?)` returns `{ ko, ja, en }` instead of a single target.
- Read-only admin monitoring page for emotion tags at `/admin/reactions`. Prisma select objects renamed to `eventSelect` / `setlistItemSelect` for clarity.

### v0.8.10 (2026-04-22)
- React `set-state-in-effect` cleanup: 8 violations across 5 files cleared. `<Header>` drops the pathname-change `useEffect` (mobile `<Link>`s close menu via `onClick`). `<EventDateTime>` + new `src/hooks/useMounted.ts` replace the `useState` / `useEffect` mount gate with `useSyncExternalStore`. `useSetlistPolling` + `useImpressionPolling` use the `useState`-pair prev-prop tracking idiom to re-sync only on actual ID change rather than every fresh array reference (the load-bearing thrash bug).
- CI gains a Lint step so future regressions can't land silently.

### v0.8.9 (2026-04-22)
- Server-side dedup for reactions + impressions via `opensetlist_anon_id` cookie. Schema: `anonId String? @db.VarChar(64)` on `SetlistItemReaction` and `EventImpression`. Two partial unique indexes in `post-deploy.sql`: `(setlistItemId, reactionType, anonId) WHERE anonId IS NOT NULL` for reactions; `(rootImpressionId, anonId) WHERE anonId IS NOT NULL AND supersededAt IS NULL` (head-only) for impressions, so the supersede transaction can re-insert the same `anonId` on a new head row without P2002.
- Closes the reactions-idempotency bug (`POST /api/reactions` creating a new row on every double-click → inflated counts → skewed Phase 1A trending signal). Closes impression chain hijacking (any caller could mutate any chain by ID). Pre-builds the merge anchor for Phase 2 account signup — future signup handler claims anon-keyed rows and rewrites them to `userId` ownership.

### v0.8.8 (2026-04-22)
- Phase 1A translation glossary: auto-derived proper-noun pipeline for impression translation. New `src/lib/glossary.ts` — types + `buildArtistTerms` + cached `getArtistTerms` (1h module-scope TTL) + `assemblePairs` + `getGlossaryForEvent` (multi-artist union including guests) + `applyGlossary` / `restoreGlossary` placeholder helpers. Translate route wires glossary application around the LLM call. Fail-open: glossary fetch errors translate raw content rather than 502.
- Admin debug page `/admin/translation-debug` for inspecting the glossary pipeline. Shows all 4 stages — pairs, substituted input, raw LLM output, restored final. Bypasses both the artist-terms cache and the `ImpressionTranslation` cache.

### v0.8.7 (2026-04-21)
- CSV import override columns for parent-level `original*` fields (`originalName`, `originalShortName`, `originalCity`, `originalVenue`, `originalBio`, `originalDescription`, `originalStageName`, `originalLanguage`). New `buildOriginals` helper centralizes the precedence rule: explicit override > `<originalLanguage>_<field>` translation > preserve existing.
- `RealPersonTranslation.shortName` + `RealPerson.originalShortName` schema additions (prerequisite for the Phase 1A translation glossary). Plumbed through `post-deploy.sql` backfill, CSV import (`va_*_shortName` / `va_originalShortName`), admin form VA block.
- Admin import UI lists parent-level `original*` override columns with Korean precedence-rule notes.

### v0.8.6 (2026-04-21)
- `originalName` flipped from nullable to NOT NULL on `Artist`, `Group`, `EventSeries`, `Event`, `StageIdentity`, `RealPerson`. Locks in the parent-level identity field that v0.8.4 added and v0.8.5 wired admin forms to write — every translation-backed parent now has a guaranteed identity string for the bleed-safe display fallback chain.
- Secondary `original*` fields (`originalShortName`, `originalBio`, `originalDescription`, `originalCity`, `originalVenue`, `originalStageName`) stay nullable because their translation-table counterparts are nullable — a real event may have no short name; a real person may have no stage name.
- Import-route create branches reworked: `requireOriginalSource()` narrows the source variable to non-null at the create site; pre-computed spread variables replaced with explicit field assignments to satisfy the new NOT NULL column.

### v0.8.5 (2026-04-21)
- Admin form scaffolding for parent-level `original*` fields. POST / PUT routes for `Artist` (incl. nested `StageIdentity` + `RealPerson`), `EventSeries`, `Event`, `Group` write parent `original*` + `originalLanguage`. Sets up v0.8.6 to flip `originalName` to NOT NULL once prod data is fully backfilled.
- Defense-in-depth on admin POST / PUT: `parseJsonBody` normalizes malformed JSON to 400, typed validators replace `as`-casts (`enumValue`, `nullableEnumValue`, `nullableString`, `nullableBigIntId` w/ `Number.isSafeInteger`, `nullableBoolean`, `nullableStringArray`). `resolveAdminSlug` trims and normalizes admin-supplied slugs with a `prefix-{timestamp}` fallback. Stage-identity slugs append `randomUUID().slice(0,8)` to avoid unique-constraint collisions; `va-{siSlug}` inherits the suffix. Atomic transactions for delete + update flows; blank-string rejection on `performerIds` / `guestIds` / `stageIdentityId`.

### v0.8.4 (2026-04-21)
- Schema: `originalName` / `originalShortName` / `originalBio` / `originalDescription` / `originalCity` / `originalVenue` (per entity) + `originalLanguage` added to `Artist`, `Group`, `EventSeries`, `Event`, `StageIdentity`, `RealPerson`. `post-deploy.sql` backfills from the matching-locale translation row. `Album.originalLanguage` normalized (`jp` → `ja`) + `originalTitle` backfilled, plus an orphan-guard that raises a Postgres `WARNING` (never an error) for any parent row still missing its identity field.
- New `displayOriginalName`, `displayNameWithFallback`, `resolveLocalizedField` helpers replace ~60 strict-`pickTranslation` call sites across event / artist / series / member / song pages, OG routes, and components. A ja viewer with no ja translation row now sees the parent `original*` values instead of a ko or en bleed-through.

### v0.8.3 (2026-04-21)
- Event OG card: `EventSeries` short name is now the headline; the specific event name moves to the subtitle (matches page H1 composition).
- OG card overflow: smaller title / subtitle fonts + 2-line clamp (`-webkit-box` + `WebkitLineClamp`) so long KR / JP event names no longer overflow the glass panel.
- New `pickLocaleTranslation` helper (strict locale match, no ko / en fallback) for Song / Album fields that have parent-level `originalTitle` / `variantLabel` — a JP viewer no longer sees a KR `variantLabel` leak through for a JP song with only a ko translation.
- `displayOriginalTitle` rewritten to take the full translations array and do the locale-exact match internally.
- Base-version link on song page renders locale-aware `variantLabel` next to the base title using the same fallback rule.

### v0.8.2 (2026-04-21)
- Phase 1A GA4 custom events wired across the app (setlist_item_click, reaction toggles, etc.) for the 2026-05-02 launch-cohort revisit-rate KPI.
- First-visit cohort cookie identifies launch-day visitors for retention tracking.
- Removed Sentry verification scaffolding (`/api/sentry-example-api`, `/sentry-example-page`, `ENABLE_SENTRY_VERIFICATION_ROUTES` flag) after prod-alert-email confirmation.

### v0.8.1 (2026-04-20)
- Admin impression moderation UI at `/admin/impressions` with 숨김 / 정상 / 삭제 / 전체 filter tabs; default 숨김 tab sorted by `reportCount desc` surfaces auto-hidden chains (reportCount ≥ 3) on top.
- `DeleteImpressionButton` + `RestoreImpressionButton` wire to the existing DELETE / PATCH `/api/admin/impressions/[chainId]` endpoints (replaces prior DevTools-fetch moderation workflow).
- Sidebar nav entry 한줄평 added between 이벤트 and CSV 가져오기.
- Sentry error tracking via `@sentry/nextjs` on Next 16.2.2 with `withSentryConfig(withNextIntl(...))` outer-wrap, prod-only gate, 10% trace sampling, `tunnelRoute: "/monitoring"` to bypass ad-blockers.
- Server `beforeSend` coerces BigInt → string so Prisma results don't crash Sentry's serializer.
- Client uses `NEXT_PUBLIC_VERCEL_ENV` (Next.js does not inline plain `VERCEL_ENV` into the browser bundle).

### v0.8.0 (2026-04-20)
- Per-cell Translate button on impressions — toggles between original and viewer-locale translation, no re-fetch on subsequent toggles.
- `POST /api/impressions/translate` with server-side cache (new `ImpressionTranslation` table keyed on `impressionId + sourceLocale + targetLocale`, naturally per-version against the append-only chain); P2002 race re-SELECTs the winner instead of erroring.
- Provider abstraction (`src/lib/translator`): OpenAI Responses API (gpt-4o-mini) and Google Gemini (gemini-3.1-flash-lite-preview) behind a single `Translator` interface; `TRANSLATION_PROVIDER` selects (Vercel preview = Gemini, prod = OpenAI).
- Hardening: shared JSON-shaped system prompt, max-token truncation detection on both providers, 256-token floor, `AbortSignal.timeout(30s)`, error-payload redaction in logs.
- Schema: new `impression_translations` table with `onDelete: Cascade` FK to `event_impressions`.

### v0.7.1 (2026-04-19)
- **Production hotfix:** `/api/og/*` routes returned 404 on prod after v0.7.0 because neither the self-hosted CJK WOFFs nor the `@vercel/og` runtime assets (`index.node.js`, `resvg.wasm`, `yoga.wasm`, `Geist-Regular.ttf`) were traced into the Vercel function bundle. Fixed by declaring `outputFileTracingIncludes` for `/api/og/**` pointing at Next's compiled `@vercel/og` copy.
- Testing safety net: vitest + jsdom + testing-library scaffold; unit tests pin Phase 1A invariants on impressions, reactions, admin-impressions, and `useSetlistPolling`.
- CI workflow runs lint + typecheck + vitest on every push.
- Semgrep workflow for static analysis.
- `migrate-prod.yml` passes `accept_data_loss` via env (fixes shell-injection finding).

### v0.7.0 (2026-04-19)
- Dynamic OG cards on Event, Song, and Artist pages with data-derived glassmorphism palette (culori OKLCH mesh, 3-branch faithful / harmonized / fallback derivation); self-hosted DM Sans + Noto Sans KR / JP for CJK glyph support on Twitter / KakaoTalk / Discord previews. SHA-256 palette fingerprint in the OG URL doubles as CDN cache-buster.
- `EventImpression` migrated to an append-only, versioned chain model — every edit creates a new row, the head row is the live impression, prior rows carry `supersededAt`. Read APIs filter `supersededAt = null`.
- Realtime impression polling on ongoing events; LIVE badge on the impressions header.
- New `/artists` and `/events` index pages with pagination; home redesigned with ongoing / upcoming / completed sections.
- Admin `EventForm` rewrite with shared validation (`src/app/api/admin/events/_validate.ts`).
- Shared OG helpers extracted: `ogFonts` (module-scope cache + in-flight dedupe), `ogLabels` (locale + status label tables), `ogPalette` (per-entity collectors).
- Cache-Control for the Event OG route now tracks the status pill — max-age capped at seconds-until-next-transition for upcoming / ongoing (no SWR), full hour + SWR for terminal states.
- Error fallback on OG routes serves `no-store` so a transient Prisma / font-load blip can't poison CDN and crawler caches with the generic OPENSETLIST card.

### v0.6.0 (2026-04-19)
- `/[locale]/events` list page grouped by tour; `/[locale]/artists` list page.
- Venue-pinned date + viewer-local time on event cards (`formatToParts`-based).
- Home page narrowed to ±30-day event window (UTC-day-aligned).
- Admin event form parity with CSV import (full event + performer + guest CRUD).
- Mobile viewport meta so `/[locale]` renders at device width; pagination wraps on narrow viewports; stacked date + title in event cards on mobile.
- Admin events API hardening (PR #48 + 5 CR rounds): reject malformed / duplicate translation payloads with 400 instead of 500; validate `eventSeriesId` is digits-only before `BigInt()`; `ensureStageIdentitiesExist` runs inside the same `$transaction` as the FK writes.

### v0.5.3 (2026-04-17)
- Real-time setlist polling: `GET /api/setlist` polled every 5s when event is ongoing; new `useSetlistPolling` hook + `LiveSetlist` client wrapper with pulsing LIVE badge.
- Event one-line impressions ("한줄평"): new `EventImpression` table (UUID id, soft-delete, 3-report auto-hide), public POST / PUT / report routes with 60s edit cooldown, admin soft-delete / restore, ko / ja / en i18n.
- Re-enabled next-intl Accept-Language detection across ko / ja / en.
- Real-time impressions polling on ongoing events.

### v0.5.2 (2026-04-16)
- Privacy / terms refresh: clarified third-party processor language for IP / cookie collection (Vercel, GA4, Cloudflare); removed "anime / game" genre scope from terms; switched contact email to help@opensetlist.com across privacy, terms, footer, README, CLAUDE.md.
- Privacy Section 4 restructured across all 3 locales to align with Section 1's processor framing: operational processors (Vercel / GA / Cloudflare) listed separately from advertising partners (AdSense / AdFit).
- `CONTACT_EMAIL` hoisted into `src/lib/config.ts` as a shared constant.
- Member page & Song page Performance History: series name is now the primary link label with the event title as a parenthetical sub-label, matching the song / event / artist pages. Event link uses canonical `event.slug` so the URL stays locale-independent.

### v0.5.1 (2026-04-16)
- Hero heading replaced across ko / ja / en with the 「みんなでつなぐ、ライブの物語」 variant; subtext line removed.
- Docs: CLAUDE.md hard rule "never merge PRs yourself".

### v0.5.0 (2026-04-16)
- Home page split into ongoing / upcoming / completed sections with per-section pagination, shared `now`, page clamping, boundary alignment with the badge resolver.
- Dropped `Event.parentEventId` self-ref — multi-day shows now use shared `EventSeries` only.
- Flipped `Event.startTime` to NOT NULL; admin `EventForm` appends `Z` to datetime-local values so server parses as UTC.
- Song page promotes series name to primary title.
- Migration: required `accept_data_loss=true` on migrate-prod (drops `parentEventId` column + tightens startTime nullability). Pre-flight on prod: 0 leg containers, 0 null startTimes.

### v0.4.1 (2026-04-16)
- Promote event series name to primary title on event listings, event detail, artist history, and home page. Event name moves to a sub-label when both exist.
- Auto-compute displayed event status from `Event.startTime` with a 12h "ongoing" buffer; admin can still override via DB enum.
- Drop deprecated `upcoming` value from the `EventStatus` enum; DB default is now `scheduled`. Prod data migrated: 9 rows `upcoming → scheduled`.
- Home page computes UTC day-boundary once and shares it across upcoming and recent queries; localize event OG description via `formatDate()`.
- CLAUDE.md: codify UTC-only date handling rule.

### v0.4.0 (2026-04-16)
- New header with OpenSetlist logo, wordmark, and nav (Home / Artists / Events / Tours).
- Mobile hamburger menu with click-outside and route-change auto-close.
- Hero section on home with Josefin Sans heading and visual search bar (Phase 1C wiring).
- Event cards restyled with DM Sans typography.
- Self-hosted Josefin Sans + DM Sans fonts (no Google CDN).

### v0.3.2 (2026-04-16)
- Remove `one_time` from `EventSeriesType` enum.
- Completes three-phase enum migration: v0.3.1 added `standalone`, SQL migrated prod rows, v0.3.2 removed `one_time` from schema.

### v0.3.1 (2026-04-16)
- Emotion tags: reaction buttons (😭🔥😱🩷) on setlist items with localStorage dedup.
- Trending songs section (top 3 most-reacted songs per event).
- `EventSeriesType`: `one_time → standalone` (단독 공연 / 単独公演); legacy `one_time` auto-normalized on CSV import and form edit.
- Import error handling: separate JSON / validation / import errors (no more UI hangs); `ImportValidationError` class for actionable 400 responses.
- Trending songs filtered to song-type items only.
- Request body validation before destructuring.

### v0.3.0 (2026-04-15)
- 3-language UI support (Korean / Japanese / English) with `LanguageSwitcher`.
- Privacy policy and terms of service pages (ko / ja / en).
- Footer component with privacy, terms, contact links.
- Admin setlist builder: reorder (move up / down), insert-after, insert-at-beginning.
- Admin setlist builder: artist names displayed in item rows.
- Input validation on insert-after and swap API routes.

### v0.2.0 (2026-04-14)
- English event translations.
- Google Analytics 4 integration.
- Personal info cleanup from public docs.
- CodeRabbit automated review config.

### v0.1.0 (2026-04-14)
- Initial production release.
- Core read-only pages: Artist, Song, Event, EventSeries.
- Admin UI with CSV import and setlist builder.
- OG cards, SEO, sitemap.
- Hasunosora seed data (224 songs).
